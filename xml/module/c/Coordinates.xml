<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Coordinates</title>
    <ns>828</ns>
    <id>475</id>
    <revision>
      <id>494539</id>
      <timestamp>2013-03-05T02:15:32Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>copy from test2wiki.  Tried to import, but that doesn't presently work for Modules</comment>
      <origin>494539</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20654" sha1="r4si3mu1yt4cqnblofomy06uq0e6oe4" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local coordinates = {
    prec_dec = require "Module:Coord_prec_dec",
    precision = require "Module:Precision",
    wikitext = require "Module:Wikitext"
}
globalFrame = nil

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", coordinates.precision.round( coordinates.precision.round(coordinate * 600, 0) % 600 / 10, 0) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + coordinates.precision.prec1(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + coordinates.precision.prec1(minutes_str)
    else
        precision = coordinates.precision.prec1(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local pframe = frame:getParent()
    local args = pframe.args
    local config = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>r4si3mu1yt4cqnblofomy06uq0e6oe4</sha1>
    </revision>
    <revision>
      <id>494540</id>
      <parentid>494539</parentid>
      <timestamp>2013-03-05T02:19:17Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494540</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20600" sha1="0p18bichq7wfn43bak1xrwhrmxybp29" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local coordinates = {
    mod_math = require "Module:Math",
    wikitext = require "Module:Wikitext"
}
globalFrame = nil

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", coordinates.precision.round( coordinates.precision.round(coordinate * 600, 0) % 600 / 10, 0) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + coordinates.precision.prec1(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + coordinates.precision.prec1(minutes_str)
    else
        precision = coordinates.precision.prec1(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local pframe = frame:getParent()
    local args = pframe.args
    local config = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>0p18bichq7wfn43bak1xrwhrmxybp29</sha1>
    </revision>
    <revision>
      <id>494541</id>
      <parentid>494540</parentid>
      <timestamp>2013-03-05T02:27:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>don't use parent framing for this</comment>
      <origin>494541</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20532" sha1="soq32i1uwqkud2gpgidbagctj2jdiv5" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local coordinates = {
    mod_math = require "Module:Math",
    wikitext = require "Module:Wikitext"
}
globalFrame = nil

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", coordinates.precision.round( coordinates.precision.round(coordinate * 600, 0) % 600 / 10, 0) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + coordinates.precision.prec1(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + coordinates.precision.prec1(minutes_str)
    else
        precision = coordinates.precision.prec1(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>soq32i1uwqkud2gpgidbagctj2jdiv5</sha1>
    </revision>
    <revision>
      <id>494542</id>
      <parentid>494541</parentid>
      <timestamp>2013-03-05T02:28:50Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494542</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20544" sha1="cjjhk7qs0p4tv4jeyu6imxciocrre2h" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local coordinates = {
    mod_math = require "Module:Math",
    wikitext = require "Module:Wikitext"
}
globalFrame = nil

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", coordinates.precision.round( coordinates.precision.round(coordinate * 600, 0) % 600 / 10, 0) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + coordinates.math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + coordinates.math_mod._precision(minutes_str)
    else
        precision = coordinates.math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>cjjhk7qs0p4tv4jeyu6imxciocrre2h</sha1>
    </revision>
    <revision>
      <id>494543</id>
      <parentid>494542</parentid>
      <timestamp>2013-03-05T02:29:27Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494543</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20544" sha1="jv812n1hutcrqs0dpj2xb42afagexup" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local coordinates = {
    math_mod = require "Module:Math",
    wikitext = require "Module:Wikitext"
}
globalFrame = nil

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", coordinates.precision.round( coordinates.precision.round(coordinate * 600, 0) % 600 / 10, 0) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + coordinates.math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + coordinates.math_mod._precision(minutes_str)
    else
        precision = coordinates.math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>jv812n1hutcrqs0dpj2xb42afagexup</sha1>
    </revision>
    <revision>
      <id>494544</id>
      <parentid>494543</parentid>
      <timestamp>2013-03-05T02:31:06Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20525" sha1="0klsqweitrtqxicm1mhf1xfu5ybnn80" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", coordinates.precision.round( coordinates.precision.round(coordinate * 600, 0) % 600 / 10, 0) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + coordinates.math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + coordinates.math_mod._precision(minutes_str)
    else
        precision = coordinates.math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>0klsqweitrtqxicm1mhf1xfu5ybnn80</sha1>
    </revision>
    <revision>
      <id>494545</id>
      <parentid>494544</parentid>
      <timestamp>2013-03-05T02:31:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494545</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20543" sha1="hx3zzccbo510wgddpno3pzin3091il6" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", coordinates.precision.round( coordinates.precision.round(coordinate * 600, 0) % 600 / 10, 0) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + coordinates.math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + coordinates.math_mod._precision(minutes_str)
    else
        precision = coordinates.math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>hx3zzccbo510wgddpno3pzin3091il6</sha1>
    </revision>
    <revision>
      <id>494546</id>
      <parentid>494545</parentid>
      <timestamp>2013-03-05T02:32:43Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494546</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20507" sha1="2c9jvn6obhoywj9kz70ahlln406j0ec" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", coordinates.precision.round( coordinates.precision.round(coordinate * 600, 0) % 600 / 10, 0) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>2c9jvn6obhoywj9kz70ahlln406j0ec</sha1>
    </revision>
    <revision>
      <id>494547</id>
      <parentid>494546</parentid>
      <timestamp>2013-03-05T02:35:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494547</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20467" sha1="b4navfx67k2bmr5vol8euk66yntzoj4" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = coordinates.prec_dec.max_precision(args[1], args[3])
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.round( math.round(coordinate * 600) % 600 / 10) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>b4navfx67k2bmr5vol8euk66yntzoj4</sha1>
    </revision>
    <revision>
      <id>494548</id>
      <parentid>494547</parentid>
      <timestamp>2013-03-05T02:36:43Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494548</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20485" sha1="q7rgfpymv9u3ftxezq26lt7tebr14q3" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = coordinates.prec_dec.max_precision(args[1], args[2])
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = math.max( math_mod._precision(args[1]), math_mod._precision(args[3]) )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.round( math.round(coordinate * 600) % 600 / 10) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>q7rgfpymv9u3ftxezq26lt7tebr14q3</sha1>
    </revision>
    <revision>
      <id>494549</id>
      <parentid>494548</parentid>
      <timestamp>2013-03-05T02:39:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494549</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20504" sha1="0klw5on5gid8j7ypcaxgiynvef62mnt" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = math.max( math_mod._precision(args[1]), math_mod._precision(args[2]) );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = math.max( math_mod._precision(args[1]), math_mod._precision(args[3]) )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.round( math.round(coordinate * 600) % 600 / 10) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>0klw5on5gid8j7ypcaxgiynvef62mnt</sha1>
    </revision>
    <revision>
      <id>494550</id>
      <parentid>494549</parentid>
      <timestamp>2013-03-05T02:43:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494550</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20501" sha1="h07ctklj25w8wj33y5o4ek24heccyk7" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = math.max( math_mod._precision(args[1]), math_mod._precision(args[2]) );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local precision = math.max( math_mod._precision(args[1]), math_mod._precision(args[3]) )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), precision) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), precision) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.round( math.round(coordinate * 600) % 600 / 10) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>h07ctklj25w8wj33y5o4ek24heccyk7</sha1>
    </revision>
    <revision>
      <id>494551</id>
      <parentid>494550</parentid>
      <timestamp>2013-03-05T02:48:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494551</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20760" sha1="83holfr4rgxcv2bshaed6yqi1ce3c1r" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = math.max( math_mod._precision(args[1]), math_mod._precision(args[2]) );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.round( math.round(coordinate * 600) % 600 / 10) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (coordinates.precision.round(360000 * coordinate, -2) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>83holfr4rgxcv2bshaed6yqi1ce3c1r</sha1>
    </revision>
    <revision>
      <id>494552</id>
      <parentid>494551</parentid>
      <timestamp>2013-03-05T02:50:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494552</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20749" sha1="bcukjr1f5f8pb818d02gatxwm61hs56" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local precision = math.max( math_mod._precision(args[1]), math_mod._precision(args[2]) );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", precision)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", precision)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.round( math.round(coordinate * 600) % 600 / 10) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", ((math.round(36000000 * coordinate) / 100) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>bcukjr1f5f8pb818d02gatxwm61hs56</sha1>
    </revision>
    <revision>
      <id>494553</id>
      <parentid>494552</parentid>
      <timestamp>2013-03-05T02:51:15Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494553</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20709" sha1="14gdnjdc8ekr6qsiosnsqjeoy74dtey" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local mode = coordinates.determineMode( args[1], args[2] );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.round( math.round(coordinate * 600) % 600 / 10) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", ((math.round(36000000 * coordinate) / 100) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>14gdnjdc8ekr6qsiosnsqjeoy74dtey</sha1>
    </revision>
    <revision>
      <id>494554</id>
      <parentid>494553</parentid>
      <timestamp>2013-03-05T02:52:25Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494554</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20715" sha1="8qqbmrs7zsdfw4sofjo9hojftm5ns0i" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local mode = coordinates.determineMode( args[1], args[2] );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.round( math.round(coordinate * 600) % 600 / 10) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", ((math.floor(36000000 * coordinate + 0.5) / 100) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>8qqbmrs7zsdfw4sofjo9hojftm5ns0i</sha1>
    </revision>
    <revision>
      <id>494555</id>
      <parentid>494554</parentid>
      <timestamp>2013-03-05T02:52:56Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494555</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20727" sha1="cse16jfmz5enhyv9fegizcglks6x12f" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local mode = coordinates.determineMode( args[1], args[2] );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.floor( math.floor(coordinate * 600 + 0.5) % 600 / 10 + 0.5) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", ((math.floor(36000000 * coordinate + 0.5) / 100) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>cse16jfmz5enhyv9fegizcglks6x12f</sha1>
    </revision>
    <revision>
      <id>494556</id>
      <parentid>494555</parentid>
      <timestamp>2013-03-05T03:02:01Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494556</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20775" sha1="k0nxmlt27a90f3jywla99tmdvmvatlz" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local mode = coordinates.determineMode( args[1], args[2] );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = coordinates.precision.round( coordinate, 0) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.floor( math.floor(coordinate * 600 + 0.5) % 600 / 10 + 0.5) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", ((math.floor(36000000 * coordinate + 0.5) / 100) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>k0nxmlt27a90f3jywla99tmdvmvatlz</sha1>
    </revision>
    <revision>
      <id>494557</id>
      <parentid>494556</parentid>
      <timestamp>2013-03-05T03:14:58Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494557</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20762" sha1="mnmxt0znw2lqddv27ghzy9rvcw437zs" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local mode = coordinates.determineMode( args[1], args[2] );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = math.floor( coordinate + 0.5 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math.floor( math.floor(coordinate * 600 + 0.5) % 600 / 10 + 0.5) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", ((math.floor(36000000 * coordinate + 0.5) / 100) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>mnmxt0znw2lqddv27ghzy9rvcw437zs</sha1>
    </revision>
    <revision>
      <id>494558</id>
      <parentid>494557</parentid>
      <timestamp>2013-03-05T03:31:18Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494558</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20760" sha1="k02auwqfni3uhlpefvyp0krsc017yvv" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local mode = coordinates.determineMode( args[1], args[2] );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600) % 600 / 10 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", ((math.floor(36000000 * coordinate + 0.5) / 100) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>k02auwqfni3uhlpefvyp0krsc017yvv</sha1>
    </revision>
    <revision>
      <id>494559</id>
      <parentid>494558</parentid>
      <timestamp>2013-03-05T03:32:28Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494559</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20769" sha1="c6piiq84fkctuk06www6wpr43exxlk0" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local mode = coordinates.determineMode( args[1], args[2] );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", ((math.floor(36000000 * coordinate + 0.5) / 100) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>c6piiq84fkctuk06www6wpr43exxlk0</sha1>
    </revision>
    <revision>
      <id>494560</id>
      <parentid>494559</parentid>
      <timestamp>2013-03-05T03:34:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494560</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20764" sha1="1cfy0kscmyn7fxfy7pnoefhfanlqw1l" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec(args)
    local coordinateSpec = {}
    local errors = {}
    
    if args[2] == "" or args[2] == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = args[1]
    coordinateSpec["dec-long"] = args[2]

    local mode = coordinates.determineMode( args[1], args[2] );
    coordinateSpec["dms-lat"]  = convert_dec2dms(args[1], "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[2], "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(args[2]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec(args)
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>1cfy0kscmyn7fxfy7pnoefhfanlqw1l</sha1>
    </revision>
    <revision>
      <id>494561</id>
      <parentid>494560</parentid>
      <timestamp>2013-03-05T03:58:38Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>sane vars</comment>
      <origin>494561</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20744" sha1="1zniz7s0m0qwfvw8qalh4j0pcifwvd4" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = lat .."_N_" .. long .. "_E_" .. args[3]

    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dec"
    end
    coordinateSpec.name     = args["name"]
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        local decResult, errors = parseDec( args[1], args[2] )
        return specPrinter(args,decResult) .. " " .. errorPrinter(errors)
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        local dmsResult, errors = parseDMS(args)
        return specPrinter(args, dmsResult ) .. " " .. errorPrinter(errors)
        -- return "dms"
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        local dmResult, errors = parseDM(args)
        return specPrinter(args, dmResult ) .. " " .. errorPrinter(errors)
        -- return "dm"
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        local dResult, errors = parseD(args)
        return specPrinter(args, dResult ) .. " " .. errorPrinter(errors)
        -- return "d"
    end
    -- Error
    return errorPrinter( {{"formatTest", "Unknown argument format"}} )
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>1zniz7s0m0qwfvw8qalh4j0pcifwvd4</sha1>
    </revision>
    <revision>
      <id>494562</id>
      <parentid>494561</parentid>
      <timestamp>2013-03-05T04:04:22Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494562</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20395" sha1="ohlsswnphlg8zwm3pdrz2a3ocirr8lk" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS(args)
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDM(args)
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseD(args)
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>ohlsswnphlg8zwm3pdrz2a3ocirr8lk</sha1>
    </revision>
    <revision>
      <id>494563</id>
      <parentid>494562</parentid>
      <timestamp>2013-03-05T04:08:41Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494563</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20481" sha1="o1fmngufzle2gqp82mr3sbwu8xkskxd" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, spec, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = lat .."_N_" .. long .. "_E_" .. spec
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS(args)
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. optionalArg(args[3],"″") .. args[4]
    coordinateSpec["dms-long"] = args[5].."°"..optionalArg(args[6],"′") .. optionalArg(args[7],"″") .. args[8]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[4],args[1],args[2],args[3]) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[8],args[5],args[6],args[7]) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name     = args["name"]

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'], args[3] )
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS(args)
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDM(args)
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseD(args)
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>o1fmngufzle2gqp82mr3sbwu8xkskxd</sha1>
    </revision>
    <revision>
      <id>494564</id>
      <parentid>494563</parentid>
      <timestamp>2013-03-05T04:14:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>sane vars</comment>
      <origin>494564</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20459" sha1="5ghg5n6uyijkwg06y5qyntstun1ls5h" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, spec, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = lat .."_N_" .. long .. "_E_" .. spec
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format, spec )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = lat_d .. "_" .. lat_m .. "_" .. lat_s .. "_" .. lat_f .. "_".. long_d .. "_" .. long_m .. "_" .. long_s .. "_" .. long_f .. "_" .. spec
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end

    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end

    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'], args[3] )
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS(args)
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDM(args)
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseD(args)
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>5ghg5n6uyijkwg06y5qyntstun1ls5h</sha1>
    </revision>
    <revision>
      <id>494565</id>
      <parentid>494564</parentid>
      <timestamp>2013-03-05T04:16:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494565</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20571" sha1="9uou7bfknb4fhji4f7noj614j2xvru3" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, spec, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = lat .."_N_" .. long .. "_E_" .. spec
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format, spec )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = lat_d .. "_" .. lat_m .. "_" .. lat_s .. "_" .. lat_f .. "_".. long_d .. "_" .. long_m .. "_" .. long_s .. "_" .. long_f .. "_" .. spec
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'], args[3] )
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'], args[9] )
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDM(args)
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseD(args)
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>9uou7bfknb4fhji4f7noj614j2xvru3</sha1>
    </revision>
    <revision>
      <id>494566</id>
      <parentid>494565</parentid>
      <timestamp>2013-03-05T04:18:30Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494566</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20762" sha1="h8g8fnfo33m3szdhrlr8pk0g4lxaxn7" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, spec, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = lat .."_N_" .. long .. "_E_" .. spec
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format, spec )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = lat_d .. "_" .. lat_m .. "_" .. lat_s .. "_" .. lat_f .. "_".. long_d .. "_" .. long_m .. "_" .. long_s .. "_" .. long_f .. "_" .. spec
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'], args[3] )
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'], args[9] )
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'], args[7] )
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'], args[5] )
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>h8g8fnfo33m3szdhrlr8pk0g4lxaxn7</sha1>
    </revision>
    <revision>
      <id>494567</id>
      <parentid>494566</parentid>
      <timestamp>2013-03-05T04:21:23Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494567</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="21015" sha1="sodj8kvyo8r04aspw8rzxafko5a0zw8" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, spec, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = lat .."_N_" .. long .. "_E_" .. spec
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format, spec )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    -- TODO Use loop when we know it won't break everything
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

function parseDM(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dms-lat"]  = args[1].."°"..optionalArg(args[2],"′") .. args[3]
    coordinateSpec["dms-long"] = args[4].."°"..optionalArg(args[5],"′") .. args[6]
    coordinateSpec["dec-lat"]  = convert_dms2dec(args[3],args[1],args[2]) -- {{coord/dms2dec|{{{3}}}|{{{1}}}|0{{{2}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(args[6],args[4],args[5]) -- {{coord/dms2dec|{{{6}}}|{{{4}}}|0{{{5}}}}}
    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7]
    if args["format"] ~= "" then
        coordinateSpec.default = args["format"]
    else
        coordinateSpec.default = "dms"
    end
    coordinateSpec.name = args["name"]
    
    -- Error reporting
    if isEmpty(args[4]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not (isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDM", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDM", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "latm&lt;0"})
    end
    if (tonumber(args[4]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDM", "longd&gt;=360"})
    end
    if (tonumber(args[4]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDM", "longd&lt;=-360"})
    end
    if (tonumber(args[5]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDM", "longm&gt;=60"})
    end
    if (tonumber(args[5]) or 0) &lt; 0 then
        table.insert(errors, {"parseDM", "longm&lt;0"})
    end
    
    return coordinateSpec, errors
end

function parseD(args)
    local coordinateSpec = {}
    local errors = {}

    coordinateSpec["dec-lat"]  = args[1]
    if args[2] =="S" then
        coordinateSpec["dec-lat"] = "-" .. coordinateSpec["dec-lat"]
    end
    coordinateSpec["dec-long"]  = args[4]
    if args[4] =="W" then
        coordinateSpec["dec-long"] = "-" .. coordinateSpec["dec-long"]
    end
        
    coordinateSpec["dec-lat-display"] = args[1] .. "°" .. args[2]
    coordinateSpec["dec-long-display"] = args[3] .. "°" .. args[4]
        
    local function postfixInverter(NE, latlong)
        if NE == "N" and latlong == "lat" then
            return "S"
        elseif NE == "E" and latlong == "long" then
            return "W"
        elseif latlong == "lat" then
            return "N"
        else
            return "E"
        end
    end

    local mode = coordinates.determineMode( args[1], args[3] )
    coordinateSpec["dms-lat"] = convert_dec2dms(args[1], args[2], postfixInverter(args[2],"lat"), mode) -- {{coord/dec2dms|{{{1}}}|{{{2}}}|{{#ifeq:{{{2}}}|N|S|N}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms(args[3], args[4], postfixInverter(args[4],"long"), mode) -- {{coord/dec2dms|{{{3}}}|{{{4}}}|{{#ifeq:{{{4}}}|E|W|E}}|{{coord/prec dec|{{{1}}}|{{{3}}}}}}}

    -- TODO Use loop when we know it won't break everything
    coordinateSpec.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5]
        
    if args["format"] ~= "" then      
        coordinateSpec.default = args["format"]
    else
        -- {{#ifeq:{{coord/prec dec|{{{1}}}|{{{3}}}}}|d|dms|dec}}
        if precision == "d" then
            coordinateSpec.default = "dms"
        else
            coordinateSpec.default = "dec"
        end
    end
    coordinateSpec.name     = args["name"]
    
    -- Error reporting
    if isEmpty(args[3]) then
        table.insert(errors, {"parseD", "Missing longitude" })
        args[3] = 0 -- to avoid error in tonumber() later on
    end

    if not (isEmpty(args[6]) and isEmpty(args[7]) and isEmpty(args[8]) and isEmpty(args[9]) and isEmpty(args[10])) then
        table.insert(errors, {"parseD", "Unexpected extra parameters"})
    end

    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseD", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseD", "latd&lt;-90"})
    end
    if (tonumber(args[3]) or 0) &gt;= 360 then
        table.insert(errors, {"parseD", "longd&gt;=360"})
    end
    if (tonumber(args[3]) or 0) &lt;= -360 then
        table.insert(errors, {"parseD", "longd&lt;=-360"})
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'], args[3] )
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'], args[9] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'], args[7] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7];
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'], args[5] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5];
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>sodj8kvyo8r04aspw8rzxafko5a0zw8</sha1>
    </revision>
    <revision>
      <id>494568</id>
      <parentid>494567</parentid>
      <timestamp>2013-03-05T04:23:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>kill DM and D versions, don't need to repeat the same logic over and over</comment>
      <origin>494568</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16236" sha1="5av3ej9wtxez437h0dneuckitq63k1s" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, spec, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec.param    = lat .."_N_" .. long .. "_E_" .. spec
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format, spec )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'], args[3] )
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'], args[9] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'], args[7] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7];
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'], args[5] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5];
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>5av3ej9wtxez437h0dneuckitq63k1s</sha1>
    </revision>
    <revision>
      <id>494569</id>
      <parentid>494568</parentid>
      <timestamp>2013-03-05T04:25:36Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494569</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16195" sha1="rvmdz405uc924iecxhjpm8epatf4q39" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = args[1] .."_N_" .. args[2] .. "_E_" .. args[3];
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7] .. "_" .. args[8] .. "_" .. args[9]
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5] .. "_" .. args[6] .. "_" .. args[7];
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = args[1] .. "_" .. args[2] .. "_" .. args[3] .. "_" .. args[4] .. "_".. args[5];
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>rvmdz405uc924iecxhjpm8epatf4q39</sha1>
    </revision>
    <revision>
      <id>494570</id>
      <parentid>494569</parentid>
      <timestamp>2013-03-05T04:31:15Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>fmt</comment>
      <origin>494570</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16001" sha1="qnks4mi1raud3xe3qebnuixrt95ncwr" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { unpack( args, 1, 9 ) }, '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { unpack( args, 1, 7 ) }, '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { unpack( args, 1, 5 ) }, '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>qnks4mi1raud3xe3qebnuixrt95ncwr</sha1>
    </revision>
    <revision>
      <id>494571</id>
      <parentid>494570</parentid>
      <timestamp>2013-03-05T04:36:31Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494571</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16013" sha1="sxk84je9qdqxxl7i66k7gt74wp1whou" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = 1 --table.concat( { unpack( args, 1, 9 ) }, '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = 1 --table.concat( { unpack( args, 1, 7 ) }, '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = 1 --table.concat( { unpack( args, 1, 5 ) }, '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>sxk84je9qdqxxl7i66k7gt74wp1whou</sha1>
    </revision>
    <revision>
      <id>494572</id>
      <parentid>494571</parentid>
      <timestamp>2013-03-05T04:38:16Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494572</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16151" sha1="ittbqqzqmpcfoup0mohumuay2wslq4y" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math_mod._precision(seconds_str)
    elseif not isEmpty(minutes_str) then
        precision = 3 + math_mod._precision(minutes_str)
    else
        precision = math_mod._precision(degrees_str)
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>ittbqqzqmpcfoup0mohumuay2wslq4y</sha1>
    </revision>
    <revision>
      <id>494573</id>
      <parentid>494572</parentid>
      <timestamp>2013-03-05T04:40:35Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494573</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16199" sha1="7ut9o9cxffxmbw3pd8qrj98gftfiet0" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end
        
    -- TODO refactor the validations into separate functions
    if (tonumber(lat) or 0) &gt; 90 then
        table.insert(errors, {"parseDec","latd&gt;90"})
    end
    if (tonumber(lat) or 0) &lt; -90 then
        table.insert(errors, {"parseDec", "latd&lt;-90"})
    end
    if (tonumber(long) or 0) &gt;= 360 then
        table.insert(errors, {"parseDec", "longd&gt;=360"})
    end
    if (tonumber(long) or 0) &lt;= -360 then
        table.insert(errors, {"parseDec", "longd&lt;=-360"})
    end
    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
--[[
    -- Error reporting
    if isEmpty(args[5]) then
        table.insert(errors, {"parseDM", "Missing longitude" })
    end

    if not isEmpty(args[10])  then
        table.insert(errors, {"parseDM", "Unexpected extra parameters"})
    end
    
    if (tonumber(args[1]) or 0) &gt; 90 then
        table.insert(errors, {"parseDMS", "latd&gt;90"})
    end
    if (tonumber(args[1]) or 0) &lt; -90 then
        table.insert(errors, {"parseDMS", "latd&lt;-90"})
    end
    if (tonumber(args[2]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "latm&gt;=60"})
    end
    if (tonumber(args[2]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "latm&lt;0"})
    end
    if (tonumber(args[3]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "lats&gt;=60"})
    end
    if (tonumber(args[3]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "lats&lt;0"})
    end
    if (tonumber(args[5]) or 0) &gt;= 360 then
        table.insert(errors, {"parseDMS", "longd&gt;=360"})
    end
    if (tonumber(args[5]) or 0) &lt;= -360 then
        table.insert(errors, {"parseDMS", "longd&lt;=-360"})
    end
    if (tonumber(args[6]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longm&gt;=60"})
    end
    if (tonumber(args[6]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longm&lt;0"})
    end
    if (tonumber(args[7]) or 0) &gt;= 60 then
        table.insert(errors, {"parseDMS", "longs&gt;=60"})
    end
    if (tonumber(args[7]) or 0) &lt; 0 then
        table.insert(errors, {"parseDMS", "longs&lt;0"})
    end
]]
    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

--- TODO not yet in use
function validateDegreesLatitude(degrees)
    if 0+tonumber(degrees) &gt; 90 then
        return "latd&gt;90"
    end
    if 0+tonumber(degrees) &lt; -90 then
        return "latd&lt;-90"
    end
    return true
end

--- TODO not yet in use
function validateDegreesLongtitude(degrees)
    if 0+tonumber(degrees) &gt;= 360 then
        return "longd&gt;=360"
    end
    if 0+tonumber(degrees) &lt;= -360 then
        return "longd&lt;=-360"
    end
    return true
end

--- TODO not yet in use
function validateMinutes(minutes)
    if 0+tonumber(minutes) &gt;= 60 then
        return "m&gt;=60"
    end
    if 0+tonumber(minutes) &lt; 0 then
        return "m&lt;0"
    end
    return true
end

--- TODO not yet in use
function validateSeconds(seconds)
    if 0+tonumber(seconds) &gt;= 60 then
        return "s&gt;=60"
    end
    if 0+tonumber(seconds) &lt; 0 then
        return "s&lt;0"
    end
    return true
end

--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>7ut9o9cxffxmbw3pd8qrj98gftfiet0</sha1>
    </revision>
    <revision>
      <id>494574</id>
      <parentid>494573</parentid>
      <timestamp>2013-03-05T04:51:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>move validation functions</comment>
      <origin>494574</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="14882" sha1="i88pf69uwaanb5k45us14fzqbbp0c8p" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    -- TODO i18n
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>i88pf69uwaanb5k45us14fzqbbp0c8p</sha1>
    </revision>
    <revision>
      <id>494575</id>
      <parentid>494574</parentid>
      <timestamp>2013-03-05T05:24:44Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494575</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="14955" sha1="p7vadeul9pqdv1lnhw1pn3up00p7j8t" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. coordinateSpec["dms-lat"] 
             .. coordinateSpec["dms-long"]
--             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
--             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>p7vadeul9pqdv1lnhw1pn3up00p7j8t</sha1>
    </revision>
    <revision>
      <id>494576</id>
      <parentid>494575</parentid>
      <timestamp>2013-03-05T05:26:27Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494576</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15013" sha1="b26ci5dcbr6n0c14331230jlim1a1rm" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. coordinateSpec["dms-lat"] 
             .. coordinateSpec["dms-long"]
--             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
--             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat 
             .. geodeclong
--             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
--             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>b26ci5dcbr6n0c14331230jlim1a1rm</sha1>
    </revision>
    <revision>
      <id>494577</id>
      <parentid>494576</parentid>
      <timestamp>2013-03-05T05:28:09Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494577</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15025" sha1="ij41iyj35lqszmskgmuycsphokpqnme" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. coordinateSpec["dms-lat"] .. ' '
             .. coordinateSpec["dms-long"]
--             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
--             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
--             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
--             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>ij41iyj35lqszmskgmuycsphokpqnme</sha1>
    </revision>
    <revision>
      <id>494578</id>
      <parentid>494577</parentid>
      <timestamp>2013-03-05T05:29:56Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494578</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15025" sha1="lupkt43mxkpq0hry7staajzbsk05yej" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
--             .. coordinateSpec["dms-lat"] .. ' '
--             .. coordinateSpec["dms-long"]
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
--             .. '&lt;span class="latitude"&gt;' .. geodeclat .. '&lt;/span&gt; '
--             .. '&lt;span class="longitude"&gt;' .. geodeclong .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'
             
    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>lupkt43mxkpq0hry7staajzbsk05yej</sha1>
    </revision>
    <revision>
      <id>494579</id>
      <parentid>494578</parentid>
      <timestamp>2013-03-05T05:37:19Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494579</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15062" sha1="74rrooydk6lkr7dngvpou1pso40hd4h" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "num" ) .. '"&gt;﻿ / ' .. geonumhtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>74rrooydk6lkr7dngvpou1pso40hd4h</sha1>
    </revision>
    <revision>
      <id>494580</id>
      <parentid>494579</parentid>
      <timestamp>2013-03-05T05:39:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494580</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15015" sha1="fvsccgj4ni3mvunlk1h9ilxepxzx41n" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml .. '&lt;/span&gt;'
            .. '&lt;span style="display:none"&gt;﻿ / ' .. geonumhtml .. '&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>fvsccgj4ni3mvunlk1h9ilxepxzx41n</sha1>
    </revision>
    <revision>
      <id>494581</id>
      <parentid>494580</parentid>
      <timestamp>2013-03-05T05:41:19Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494581</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15010" sha1="nz0r0wqblkqubs8c03ye1lu5slmc5gq" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;﻿ / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>nz0r0wqblkqubs8c03ye1lu5slmc5gq</sha1>
    </revision>
    <revision>
      <id>494582</id>
      <parentid>494581</parentid>
      <timestamp>2013-03-05T05:49:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494582</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15015" sha1="t0zi2scl7honcq35f0s8389qrzbj1az" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>t0zi2scl7honcq35f0s8389qrzbj1az</sha1>
    </revision>
    <revision>
      <id>494583</id>
      <parentid>494582</parentid>
      <timestamp>2013-03-05T05:52:47Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494583</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15112" sha1="3dq5rez68o60rnotv7mk5b622sf30fk" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>3dq5rez68o60rnotv7mk5b622sf30fk</sha1>
    </revision>
    <revision>
      <id>494584</id>
      <parentid>494583</parentid>
      <timestamp>2013-03-05T06:37:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494584</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15157" sha1="bmporpnb2m2na3ifru1f53rklsm00he" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    table.insert( errors, { 'mode', mode } )
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>bmporpnb2m2na3ifru1f53rklsm00he</sha1>
    </revision>
    <revision>
      <id>494585</id>
      <parentid>494584</parentid>
      <timestamp>2013-03-05T06:40:07Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494585</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15134" sha1="3kqwj26gwc0kscgmlyehozjj2mnia24" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    

    coordinateSpec["dms-lat"]  = mode .. convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = mode .. convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>3kqwj26gwc0kscgmlyehozjj2mnia24</sha1>
    </revision>
    <revision>
      <id>494586</id>
      <parentid>494585</parentid>
      <timestamp>2013-03-05T06:41:08Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494586</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15158" sha1="nsyskir4xov5l03p6w4wkp301yv607d" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = mode .. convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = mode .. convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    
    error( 'Stop now' );
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>nsyskir4xov5l03p6w4wkp301yv607d</sha1>
    </revision>
    <revision>
      <id>494587</id>
      <parentid>494586</parentid>
      <timestamp>2013-03-05T06:43:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494587</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15180" sha1="nvwkudaao4ltbiyu63v8zmgvlud7v8n" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    coordinateSpec["dec-lat"]  =     coordinateSpec["dms-lat"];
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>nvwkudaao4ltbiyu63v8zmgvlud7v8n</sha1>
    </revision>
    <revision>
      <id>494588</id>
      <parentid>494587</parentid>
      <timestamp>2013-03-05T06:44:44Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494588</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15155" sha1="pxnvireukff2ikmdvavcoih0qmlrqnd" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    coordinateSpec["dec-lat"]  = 'hih'
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>pxnvireukff2ikmdvavcoih0qmlrqnd</sha1>
    </revision>
    <revision>
      <id>494589</id>
      <parentid>494588</parentid>
      <timestamp>2013-03-05T07:00:22Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494589</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15473" sha1="0583rooev592o8hxuwkfdnybmejvh9g" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = -lat_d;
        end
        if long_f:upper() == 'E' then 
            long_d = -long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>0583rooev592o8hxuwkfdnybmejvh9g</sha1>
    </revision>
    <revision>
      <id>494590</id>
      <parentid>494589</parentid>
      <timestamp>2013-03-05T07:01:23Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494590</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15473" sha1="puue2k519iys8gege08uu68wbg0nx9n" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = -lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = -long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>puue2k519iys8gege08uu68wbg0nx9n</sha1>
    </revision>
    <revision>
      <id>494591</id>
      <parentid>494590</parentid>
      <timestamp>2013-03-05T07:14:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494591</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15493" sha1="ckoyixzklh57yhsuejnzi7kjaqpj7vm" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = -tonumber(lat_d);
        end
        if long_f:upper() == 'W' then 
            long_d = -tonumber(long_d);
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>ckoyixzklh57yhsuejnzi7kjaqpj7vm</sha1>
    </revision>
    <revision>
      <id>494592</id>
      <parentid>494591</parentid>
      <timestamp>2013-03-05T07:16:42Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494592</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15485" sha1="3nj287fruyh4fo55l97l5g2yucb5rbl" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 600, 0) % 600 / 10, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor(60 * coordinate) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>3nj287fruyh4fo55l97l5g2yucb5rbl</sha1>
    </revision>
    <revision>
      <id>494593</id>
      <parentid>494592</parentid>
      <timestamp>2013-03-05T07:27:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494593</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15488" sha1="0f159epr72kfup6x7wjbdy7smf8s4bj" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 60, 0) % 60, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math_mod._round( 60 * coordinate, 0 ) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>0f159epr72kfup6x7wjbdy7smf8s4bj</sha1>
    </revision>
    <revision>
      <id>494594</id>
      <parentid>494593</parentid>
      <timestamp>2013-03-05T07:30:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494594</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15480" sha1="3sizasbx665tad5lahn0aa3d4qp4bb6" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    local d = math.floor(coordinate % 360) .."°"
    local m = string.format( "%02d′", math_mod._round( math_mod._round(coordinate * 60, 0) % 60, 0 ) )
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor( 60 * coordinate ) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>3sizasbx665tad5lahn0aa3d4qp4bb6</sha1>
    </revision>
    <revision>
      <id>494595</id>
      <parentid>494594</parentid>
      <timestamp>2013-03-05T07:39:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494595</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15504" sha1="hj754ym4e41986y50ih28x3w0qcyylv" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)
    -- {{#expr:{{{1}}} mod 360}}°{{padleft:{{#expr:({{{1}}} * 600 round 0) mod 600 / 10 round 0}}|2|0}}′
    
    coordinate = coordinate * 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    --{{#expr:(((({{{1|0}}}) * 3600) round 0) / 3600) mod 360}}°{{padleft:{{#expr:(((3600 * ({{{1|0}}})) round 0) / 60) mod 60}}|2|0}}′{{padleft:{{#expr:((360000 * ({{{1|0}}})) round -2) mod 6000 div 100}}|2|0}}″
    local d = math.floor(coordinate % 360) .. "°"
    local m = string.format( "%02d′", math.floor( 60 * coordinate ) % 60 )
    local s = string.format( "%02d″", (math_mod._round( 360000 * coordinate, -2 ) % 6000) / 100 )

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>hj754ym4e41986y50ih28x3w0qcyylv</sha1>
    </revision>
    <revision>
      <id>494596</id>
      <parentid>494595</parentid>
      <timestamp>2013-03-05T07:40:56Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494596</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15218" sha1="4z86w5klsphs65asatx7vul4a5lnqge" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = coordinate * 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"
    
    return d .. m
end

function convert_dec2dms_dms(coordinate)
    coordinate = coordinate * 60 * 60;
    local s = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - s) / 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"

    return d .. m .. s
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>4z86w5klsphs65asatx7vul4a5lnqge</sha1>
    </revision>
    <revision>
      <id>494597</id>
      <parentid>494596</parentid>
      <timestamp>2013-03-05T07:43:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494597</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15302" sha1="s1hvh7aabs973sxjyutnvm83qxdy4ca" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = coordinate * 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = coordinate * 60 * 60;
    local s = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - s) / 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d′", m )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>s1hvh7aabs973sxjyutnvm83qxdy4ca</sha1>
    </revision>
    <revision>
      <id>494598</id>
      <parentid>494597</parentid>
      <timestamp>2013-03-05T07:44:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494598</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15302" sha1="2we9ab86v9f8qp1qceoxr444c111a5v" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = coordinate * 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = coordinate * 60 * 60;
    local s = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - s) / 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d′", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>2we9ab86v9f8qp1qceoxr444c111a5v</sha1>
    </revision>
    <revision>
      <id>494599</id>
      <parentid>494598</parentid>
      <timestamp>2013-03-05T07:45:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494599</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15302" sha1="4to16b1lg11n6x8vx8pehrzdx5xumwf" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        coordinateSpec, errors = parseDec( lat_d, long_d, format );
    else    
        coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
        coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
        coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
        coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}
    end

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = coordinate * 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = coordinate * 60 * 60;
    local s = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - s) / 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>4to16b1lg11n6x8vx8pehrzdx5xumwf</sha1>
    </revision>
    <revision>
      <id>494600</id>
      <parentid>494599</parentid>
      <timestamp>2013-03-05T07:47:38Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494600</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15263" sha1="goscxf7okudlrphi6yfkfzn5b4fnotx" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = coordinate * 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = coordinate * 60 * 60;
    local s = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - s) / 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>goscxf7okudlrphi6yfkfzn5b4fnotx</sha1>
    </revision>
    <revision>
      <id>494601</id>
      <parentid>494600</parentid>
      <timestamp>2013-03-05T07:50:11Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494601</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15255" sha1="2ichxesrbtcbv1ptt49zun9hnu856v0" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = coordinate * 60;
    local m = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = coordinate * 60 * 60;
    local s = math_mod._round(coordinate, 0) % 60
    coordinate = (coordinate - s) / 60;
    local m = math.floor(coordinate % 60)
    coordinate = (coordinate - m) / 60;
    local d = math.floor(coordinate % 360) .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>2ichxesrbtcbv1ptt49zun9hnu856v0</sha1>
    </revision>
    <revision>
      <id>494602</id>
      <parentid>494601</parentid>
      <timestamp>2013-03-05T07:57:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494602</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15266" sha1="b7r8aa1iixjjasxsnifyle718rg26f9" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>b7r8aa1iixjjasxsnifyle718rg26f9</sha1>
    </revision>
    <revision>
      <id>494603</id>
      <parentid>494602</parentid>
      <timestamp>2013-03-05T08:08:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494603</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15246" sha1="h63quznsgs1n7os98xhohshc950thgk" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>h63quznsgs1n7os98xhohshc950thgk</sha1>
    </revision>
    <revision>
      <id>494604</id>
      <parentid>494603</parentid>
      <timestamp>2013-03-05T08:09:14Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494604</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15266" sha1="b7r8aa1iixjjasxsnifyle718rg26f9" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%s+", '%%20'):gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess('[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' .. uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>b7r8aa1iixjjasxsnifyle718rg26f9</sha1>
    </revision>
    <revision>
      <id>494605</id>
      <parentid>494604</parentid>
      <timestamp>2013-03-05T08:13:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494605</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15283" sha1="l7xe357459tg0oj2i23tvdj6gifam5k" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return arg:gsub("%&lt;", "%%3C"):gsub("%&gt;", "%%3E")
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        '{{urlencode:' .. uriComponents .. '}} ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>l7xe357459tg0oj2i23tvdj6gifam5k</sha1>
    </revision>
    <revision>
      <id>494606</id>
      <parentid>494605</parentid>
      <timestamp>2013-03-05T08:16:45Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494606</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15243" sha1="h4s2lspwvazek70hoj1v1i1tx86jurw" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    -- TODO requires vcard
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
            .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
            .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
            .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    
    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>h4s2lspwvazek70hoj1v1i1tx86jurw</sha1>
    </revision>
    <revision>
      <id>494607</id>
      <parentid>494606</parentid>
      <timestamp>2013-03-05T08:24:48Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494607</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15884" sha1="6ryo4r5fqjyrteh1th3u66jlqavu6wq" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="vcard"&gt;&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>6ryo4r5fqjyrteh1th3u66jlqavu6wq</sha1>
    </revision>
    <revision>
      <id>494608</id>
      <parentid>494607</parentid>
      <timestamp>2013-03-05T08:25:47Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494608</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15872" sha1="0j5ot7x0jg99ry3bkifcpt6p0cjrg2b" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="vcard"&gt;&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>0j5ot7x0jg99ry3bkifcpt6p0cjrg2b</sha1>
    </revision>
    <revision>
      <id>494609</id>
      <parentid>494608</parentid>
      <timestamp>2013-03-05T08:28:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494609</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15872" sha1="1jrdi3bds310wungftvtuxuvlr6xqy9" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if lat_f:upper() == 'S' then 
            lat_d = '-' .. lat_d;
        end
        if long_f:upper() == 'W' then 
            long_d = '-' .. long_d;
        end     
        
        return parseDec( lat_d, long_d, format );
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>1jrdi3bds310wungftvtuxuvlr6xqy9</sha1>
    </revision>
    <revision>
      <id>494610</id>
      <parentid>494609</parentid>
      <timestamp>2013-03-05T08:39:27Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494610</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16010" sha1="n87476ymr30b039vncb5yk6mvq8u5eb" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = args.display or "inline"
    local text
    if ( "title" ~= Display ) then
        text = displayinline(contents)
    else
        text = displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>n87476ymr30b039vncb5yk6mvq8u5eb</sha1>
    </revision>
    <revision>
      <id>494611</id>
      <parentid>494610</parentid>
      <timestamp>2013-03-05T08:45:16Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494611</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16099" sha1="lip3llvnud3xuykiqw4grfbbn71rrf5" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text
    if string.find( Display, 'inline' ) ~= nil then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>lip3llvnud3xuykiqw4grfbbn71rrf5</sha1>
    </revision>
    <revision>
      <id>494612</id>
      <parentid>494611</parentid>
      <timestamp>2013-03-05T08:50:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494612</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16099" sha1="dwl5hxqdr19m2ju6vabhb0dwdgngvsx" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text
    if string.find( Display, 'title' ) ~= nil then
        text = displaytitle(contents)
    end
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>dwl5hxqdr19m2ju6vabhb0dwdgngvsx</sha1>
    </revision>
    <revision>
      <id>494613</id>
      <parentid>494612</parentid>
      <timestamp>2013-03-05T08:50:42Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494613</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16112" sha1="hnm57vnfum7secxkbb2dm8wvq28atg6" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = mw.text.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return mw.text.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>hnm57vnfum7secxkbb2dm8wvq28atg6</sha1>
    </revision>
    <revision>
      <id>494614</id>
      <parentid>494613</parentid>
      <timestamp>2013-03-05T08:52:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494614</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16114" sha1="ecz3dk8v0z60w57i87l7dpx56hgrlqp" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";
local wikitext = require "Module:Wikitext"

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = wikitext.tag({name="span",contents=l,params={id="coordinates"}})
    local p = {}
    p["font-size"] = "small"
    return wikitext.tag({name="span",contents=co,params=p})
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>ecz3dk8v0z60w57i87l7dpx56hgrlqp</sha1>
    </revision>
    <revision>
      <id>494615</id>
      <parentid>494614</parentid>
      <timestamp>2013-03-05T08:57:24Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494615</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16003" sha1="kqp9isxyp5ixi55ol9kv7rk05joikk8" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = "&lt;span id='coordinates'&gt;" .. l .. "&lt;/span&gt;";
    return "&lt;span font-size='small'&gt;" .. co .. "&lt;/span&gt;";
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>kqp9isxyp5ixi55ol9kv7rk05joikk8</sha1>
    </revision>
    <revision>
      <id>494616</id>
      <parentid>494615</parentid>
      <timestamp>2013-03-05T09:00:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494616</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16010" sha1="dzr31upbf7nrzifl7hh03pvlsqt1f3c" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>dzr31upbf7nrzifl7hh03pvlsqt1f3c</sha1>
    </revision>
    <revision>
      <id>494617</id>
      <parentid>494616</parentid>
      <timestamp>2013-03-05T09:01:38Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494617</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16011" sha1="gobaudjofrrf6zv7pk3eu9xoer2dd6s" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if ( nil == args[i] ) then args[i] = "" end 
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>gobaudjofrrf6zv7pk3eu9xoer2dd6s</sha1>
    </revision>
    <revision>
      <id>494618</id>
      <parentid>494617</parentid>
      <timestamp>2013-03-05T16:30:38Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>whitespace</comment>
      <origin>494618</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16124" sha1="sdxbjqy2zm532c63lkybe2p5ole4e0d" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[1] = args[1]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>sdxbjqy2zm532c63lkybe2p5ole4e0d</sha1>
    </revision>
    <revision>
      <id>494619</id>
      <parentid>494618</parentid>
      <timestamp>2013-03-05T16:31:16Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>494619</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16124" sha1="9ktihxrzmai4497gypg8yum5bfetcgf" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>9ktihxrzmai4497gypg8yum5bfetcgf</sha1>
    </revision>
    <revision>
      <id>494620</id>
      <parentid>494619</parentid>
      <timestamp>2013-03-05T16:35:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494620</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16123" sha1="5xw7z66fll1xlkbbj6ezqnrxiz4swnd" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return '', {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>5xw7z66fll1xlkbbj6ezqnrxiz4swnd</sha1>
    </revision>
    <revision>
      <id>494621</id>
      <parentid>494620</parentid>
      <timestamp>2013-03-05T16:36:45Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494621</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16135" sha1="1z1ciazcjsnzryr0qpbgij7h1o6911n" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return coordinateSpec, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- BAD BAD URL escape
-- replace this later with the actual helper template
function urlEscape(arg)
    return mw.uri.encode( arg );
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. urlEscape(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local geodeclat = coordinateSpec["dec-lat-display"]
    if geodeclat == nil then
        local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
        if lat &lt; 0 then
            -- FIXME this breaks the pre-existing precision
            geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
        else
            geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
        end
    end
    
    -- FIXME ugly code duplication, but lazy :D
    local geodeclong = coordinateSpec["dec-long-display"]
    if geodeclong == nil then
        local long = tonumber( coordinateSpec["dec-long"] ) or 0
        if long &lt; 0 then
            -- FIXME does not handle unicode minus
            geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
        else
            geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
        end
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>1z1ciazcjsnzryr0qpbgij7h1o6911n</sha1>
    </revision>
    <revision>
      <id>494622</id>
      <parentid>494621</parentid>
      <timestamp>2013-03-05T16:42:06Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494622</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15786" sha1="f91o8gd9m02mvtbemojc6p6x5awa0o7" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end
        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>f91o8gd9m02mvtbemojc6p6x5awa0o7</sha1>
    </revision>
    <revision>
      <id>494623</id>
      <parentid>494622</parentid>
      <timestamp>2013-03-05T16:47:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494623</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="15899" sha1="2k25zpauta1czphf0e7zjki0tywrx17" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec' );    
    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS' );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    if tonumber(lat_d) &lt; 0 then
        table.insert( errors, { 'parseDMS', 'Invalid latitude' } );
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end
        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source )
    local errors = {};

    if (tonumber(lat_d) or 0) &gt; 90 then
        table.insert(errors, {source, "latd&gt;90"})
    end
    if (tonumber(lat_d) or 0) &lt; -90 then
        table.insert(errors, {source, "latd&lt;-90"})
    end
    if (tonumber(lat_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "latm&gt;=60"})
    end
    if (tonumber(lat_m) or 0) &lt; 0 then
        table.insert(errors, {source, "latm&lt;0"})
    end
    if (tonumber(lat_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "lats&gt;=60"})
    end
    if (tonumber(lat_s) or 0) &lt; 0 then
        table.insert(errors, {source, "lats&lt;0"})
    end
    if (tonumber(long_d) or 0) &gt;= 360 then
        table.insert(errors, {source, "longd&gt;=360"})
    end
    if (tonumber(long_d) or 0) &lt;= -360 then
        table.insert(errors, {source, "longd&lt;=-360"})
    end
    if (tonumber(long_m) or 0) &gt;= 60 then
        table.insert(errors, {source, "longm&gt;=60"})
    end
    if (tonumber(long_m) or 0) &lt; 0 then
        table.insert(errors, {source, "longm&lt;0"})
    end
    if (tonumber(long_s) or 0) &gt;= 60 then
        table.insert(errors, {source, "longs&gt;=60"})
    end
    if (tonumber(long_s) or 0) &lt; 0 then
        table.insert(errors, {source, "longs&lt;0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>2k25zpauta1czphf0e7zjki0tywrx17</sha1>
    </revision>
    <revision>
      <id>494624</id>
      <parentid>494623</parentid>
      <timestamp>2013-03-05T16:58:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494624</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16556" sha1="jvbuqeuy7exboprie4s6accziq4eggw" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    if tonumber(lat_d) &lt; 0 then
        table.insert( errors, { 'parseDMS', 'Invalid latitude' } );
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end
        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_d ) or 0;
    lat_s = tonumber( lat_d ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 60"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>jvbuqeuy7exboprie4s6accziq4eggw</sha1>
    </revision>
    <revision>
      <id>494625</id>
      <parentid>494624</parentid>
      <timestamp>2013-03-05T17:00:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494625</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16418" sha1="gzuzjmat6fcyaf3zovo74b26l7if2ec" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    if tonumber(lat_d) &lt; 0 then
        table.insert( errors, { 'parseDMS', 'Invalid latitude' } );
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end
        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_d ) or 0;
    lat_s = tonumber( lat_d ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 60"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>gzuzjmat6fcyaf3zovo74b26l7if2ec</sha1>
    </revision>
    <revision>
      <id>494626</id>
      <parentid>494625</parentid>
      <timestamp>2013-03-05T17:01:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494626</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16417" sha1="7f912z5ekzgc0rgs53r58d8odp9ilyx" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    if tonumber(lat_d) &lt; 0 then
        table.insert( errors, { 'parseDMS', 'Invalid latitude' } );
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end
        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_d ) or 0;
    lat_s = tonumber( lat_d ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>7f912z5ekzgc0rgs53r58d8odp9ilyx</sha1>
    </revision>
    <revision>
      <id>494627</id>
      <parentid>494626</parentid>
      <timestamp>2013-03-05T17:03:19Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494627</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16417" sha1="91da8d1uavosl2zko7mc17yelf5x7j9" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    if tonumber(lat_d) &lt; 0 then
        table.insert( errors, { 'parseDMS', 'Invalid latitude' } );
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end
        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>91da8d1uavosl2zko7mc17yelf5x7j9</sha1>
    </revision>
    <revision>
      <id>494628</id>
      <parentid>494627</parentid>
      <timestamp>2013-03-05T17:04:34Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494628</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16309" sha1="0hso541y593otlg1yka0pa9s57nruwn" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end
        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>0hso541y593otlg1yka0pa9s57nruwn</sha1>
    </revision>
    <revision>
      <id>494629</id>
      <parentid>494628</parentid>
      <timestamp>2013-03-05T17:09:42Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494629</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="16575" sha1="gsov7tu33e6kcmgwyi7yn20mw4p07uu" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end
        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>gsov7tu33e6kcmgwyi7yn20mw4p07uu</sha1>
    </revision>
    <revision>
      <id>494630</id>
      <parentid>494629</parentid>
      <timestamp>2013-03-05T17:24:09Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494630</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="17265" sha1="l4csfj8b53qm178v4r9jn0gisrbtyhr" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in extra_param do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>l4csfj8b53qm178v4r9jn0gisrbtyhr</sha1>
    </revision>
    <revision>
      <id>494631</id>
      <parentid>494630</parentid>
      <timestamp>2013-03-05T17:25:12Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494631</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="17275" sha1="o9e8pcgpgv1uczsofx0nfdpv6chajjm" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first .. second;
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>o9e8pcgpgv1uczsofx0nfdpv6chajjm</sha1>
    </revision>
    <revision>
      <id>494632</id>
      <parentid>494631</parentid>
      <timestamp>2013-03-05T17:37:16Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494632</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="17765" sha1="mzv79f68glcxcsxtnlhrnpsq4s91oh0" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>mzv79f68glcxcsxtnlhrnpsq4s91oh0</sha1>
    </revision>
    <revision>
      <id>494633</id>
      <parentid>494632</parentid>
      <timestamp>2013-03-05T17:39:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494633</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="17837" sha1="3cvmk2h2gjmbv9lbadhayh33hnc3dki" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>3cvmk2h2gjmbv9lbadhayh33hnc3dki</sha1>
    </revision>
    <revision>
      <id>494634</id>
      <parentid>494633</parentid>
      <timestamp>2013-03-05T17:49:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494634</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="17904" sha1="3xb26vm672janmx0y9hw3eygwh6lyju" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>3xb26vm672janmx0y9hw3eygwh6lyju</sha1>
    </revision>
    <revision>
      <id>494635</id>
      <parentid>494634</parentid>
      <timestamp>2013-03-05T17:53:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494635</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18098" sha1="k2iq4s1cc3c3nmh4ep11qo6d7wcr9eb" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame.getParentFrame();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>k2iq4s1cc3c3nmh4ep11qo6d7wcr9eb</sha1>
    </revision>
    <revision>
      <id>494636</id>
      <parentid>494635</parentid>
      <timestamp>2013-03-05T17:55:01Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494636</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18093" sha1="ijx92l756r0jrghaxtx6zo54rf4j3xx" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end        
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil then
        text = text .. displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>ijx92l756r0jrghaxtx6zo54rf4j3xx</sha1>
    </revision>
    <revision>
      <id>494637</id>
      <parentid>494636</parentid>
      <timestamp>2013-03-05T18:30:58Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494637</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18256" sha1="k02ktt9vf6w08cvfk8c4j7g3x1grmwl" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        if long_d &lt; -180 then
            table.insert(errors, {source, "longitude degrees &lt; -180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>k02ktt9vf6w08cvfk8c4j7g3x1grmwl</sha1>
    </revision>
    <revision>
      <id>494638</id>
      <parentid>494637</parentid>
      <timestamp>2013-03-05T18:34:42Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494638</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18122" sha1="5zh2u8oz6923latsiemt74k3du7a0q2" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>5zh2u8oz6923latsiemt74k3du7a0q2</sha1>
    </revision>
    <revision>
      <id>494639</id>
      <parentid>494638</parentid>
      <timestamp>2013-03-05T18:40:56Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494639</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18164" sha1="01vrivap41dy6hdazm7kippxcga2sfn" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " [[Category:Coord template needing repair]]" .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>01vrivap41dy6hdazm7kippxcga2sfn</sha1>
    </revision>
    <revision>
      <id>494640</id>
      <parentid>494639</parentid>
      <timestamp>2013-03-05T18:48:36Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494640</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18409" sha1="qjdizex2vijjt51u3fxpazf1al44de1" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " [[Category:Coord template needing repair]]" .. errorPrinter(errors)    
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>qjdizex2vijjt51u3fxpazf1al44de1</sha1>
    </revision>
    <revision>
      <id>494641</id>
      <parentid>494640</parentid>
      <timestamp>2013-03-05T18:50:35Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494641</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18413" sha1="9g5vtscqkr06qovtwrlopb7g58v3dfl" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Coord template needing repair]]'; 
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>9g5vtscqkr06qovtwrlopb7g58v3dfl</sha1>
    </revision>
    <revision>
      <id>494642</id>
      <parentid>494641</parentid>
      <timestamp>2013-03-05T19:27:06Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494642</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18420" sha1="s20pgqvekoq2y858ta5mp7la3k8reyi" xml:space="preserve">-- A module that mimics the functionality of Template:Coord and its sub templates
-- The attempt is to actually mimic a conversion of an often used en.wp template in the way
-- that most templates will actually be converted by the wiki users. 
-- Attempt is not really to write a nice and proper module from scratch :D
local math_mod = require "Module:Math";

globalFrame = nil
coordinates = {};

--- Replacement for {{coord/display/title}}
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--- Replacement for {{coord/display/inline}}
function displayinline (s)
    return s    
end

--- Test if the arguments imply that DMS might be in use
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--- Parse the frame assuming that it is in dec format.
-- @frame
-- @returns a table with all information needed to display coordinates
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function isEmpty(arg)
  if arg == nil or arg == "" then
    return true
  end
  return false
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if isEmpty(long_d) then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if not isEmpty(seconds_str) then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif not isEmpty(minutes_str) then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>s20pgqvekoq2y858ta5mp7la3k8reyi</sha1>
    </revision>
    <revision>
      <id>494643</id>
      <parentid>494642</parentid>
      <timestamp>2013-03-05T21:17:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>comments and formatting</comment>
      <origin>494643</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18384" sha1="2o98rm50o9qdybgcvnviod50wnloe47" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
tmeplates.  It provides several methods, including

{{#Invoke:Coordinates | coord }}

Which provides the functionality to generate a coordinate link equivalent to 
{{Coord}}

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s)
    return s    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    -- Specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--- A function that prints a table of coordinate specifications to HTML
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    -- TODO requires DEC formatting
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner
    if args["name"] == "" or args["name"] == nil then
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;' .. v[2] .. ' in Module:Coordinates.' .. v[1] .."()" .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--- Determine the required CSS class to display coordinates
-- Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
-- default is the mode as specificied by the user when calling the {{coord}} template
-- mode is the display mode (dec or dms) that we will need to determine the css class for 
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>2o98rm50o9qdybgcvnviod50wnloe47</sha1>
    </revision>
    <revision>
      <id>494644</id>
      <parentid>494643</parentid>
      <timestamp>2013-03-05T21:27:39Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>more formatting / comments</comment>
      <origin>494644</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18364" sha1="dh0x1xb2133zpphjn5h1biwdmlm18lz" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
tmeplates.  It provides several methods, including

{{#Invoke:Coordinates | coord }}

Which provides the functionality to generate a coordinate link equivalent to 
{{Coord}}

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s)
    return s    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDec

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Module:Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

-- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--- Convert a latitude or longitude to the DMS format
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    -- {{#expr:{{#switch:{{{1}}}|N|E=1|S|W=-1}}*({{{2|0}}}+({{{3|0}}}+{{{4|0}}}/60)/60) round {{{precdec|{{#if:{{{4|}}}|5|{{#if:{{{3|}}}|3|0}}}}+{{precision1|{{{4|{{{3|{{{2}}}}}}}}}}}}}}}} 
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>dh0x1xb2133zpphjn5h1biwdmlm18lz</sha1>
    </revision>
    <revision>
      <id>494645</id>
      <parentid>494644</parentid>
      <timestamp>2013-03-05T21:32:00Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>more formatting</comment>
      <origin>494645</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18495" sha1="hj6wx4gg66nmtx57uho3yf1z18o3oz8" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
tmeplates.  It provides several methods, including

{{#Invoke:Coordinates | coord }}

Which provides the functionality to generate a coordinate link equivalent to 
{{Coord}}

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s)
    return s    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDec

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end
    

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Module:Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

-- Check the arguments to determine what type of coordinates has been input
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    -- {{Coord/dec2dms/{{{4}}}|{{#ifexpr:{{{1}}} &gt;= 0||-}}{{{1}}}}}{{#ifexpr:{{{1}}} &gt;= 0|{{{2}}}|{{{3}}}}}
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
    
    -- return "" .. globalFrame:expandTemplate{ title = 'coord/dec2dms', args = {coordinate, firstPostfix, secondPostfix, precision}}
end

--- Convert DMS into a N or E decimal coordinate
-- @param coordinate direction. either "N" "S" "E" or "W"
-- @param degrees: string or number
-- @param minutes: string or number
-- @param seconds: string or number
-- @return a number with the N or E normalized decimal coordinate of the input
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    -- nil -&gt; 0
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
    
    --return "" .. globalFrame:expandTemplate{ title = 'coord/dms2dec', args = {direction, degrees, minutes, seconds}}
end

-- Check the input for out of range errors.
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end


--- The display function we exposed to Module:Coordinates
function coordinates.input(frame)
    globalFrame = frame;
    return formatTest(frame.args)
end

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--- The dec2dms function exposed to Module:Coordinates
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--- This is used by {{coord}}.
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>hj6wx4gg66nmtx57uho3yf1z18o3oz8</sha1>
    </revision>
    <revision>
      <id>494646</id>
      <parentid>494645</parentid>
      <timestamp>2013-03-06T05:27:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>more formatting / comments</comment>
      <origin>494646</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19421" sha1="pcmhv9kj7e9glbinnxmhc9813qcpzpy" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s)
    return s    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDec

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Module:Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>pcmhv9kj7e9glbinnxmhc9813qcpzpy</sha1>
    </revision>
    <revision>
      <id>494647</id>
      <parentid>494646</parentid>
      <timestamp>2013-03-06T05:30:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>tweak error label</comment>
      <origin>494647</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19414" sha1="lxvw5hutiz0zbj7nb749wzw4vjekp3i" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s)
    return s    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDec

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>lxvw5hutiz0zbj7nb749wzw4vjekp3i</sha1>
    </revision>
    <revision>
      <id>494648</id>
      <parentid>494647</parentid>
      <timestamp>2013-03-06T05:32:31Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <origin>494648</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19414" sha1="oyqnd3qorqklit1yy5uosem72wlc9k1" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s)
    return s    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>oyqnd3qorqklit1yy5uosem72wlc9k1</sha1>
    </revision>
    <revision>
      <id>494649</id>
      <parentid>494648</parentid>
      <timestamp>2013-03-06T05:42:01Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Protected Module:Coordinates: [[WP:High-risk templates|Highly visible template]] ([Edit=Block all non-admin users] (indefinite) [Move=Block all non-admin users] (indefinite))</comment>
      <origin>494648</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19414" sha1="oyqnd3qorqklit1yy5uosem72wlc9k1" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s)
    return s    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents)
    end
    return text .. Notes
end

return coordinates</text>
      <sha1>oyqnd3qorqklit1yy5uosem72wlc9k1</sha1>
    </revision>
    <revision>
      <id>494650</id>
      <parentid>494649</parentid>
      <timestamp>2013-03-13T21:36:28Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>fix note handling</comment>
      <origin>494650</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19451" sha1="27c6hutz5kqbhy8tau7b9jd1gfiiwsf" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
    return s .. notes    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents, Notes)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents, Notes)
    end
    return text
end

return coordinates</text>
      <sha1>27c6hutz5kqbhy8tau7b9jd1gfiiwsf</sha1>
    </revision>
    <revision>
      <id>494651</id>
      <parentid>494650</parentid>
      <timestamp>2013-03-29T21:15:12Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>494651</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19451" sha1="o5l65x412ffwyurw6asg0g1ut41f6oi" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
    return s .. notes    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[http://toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents, Notes)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents, Notes)
    end
    return text
end

return coordinates</text>
      <sha1>o5l65x412ffwyurw6asg0g1ut41f6oi</sha1>
    </revision>
    <revision>
      <id>494652</id>
      <parentid>494651</parentid>
      <timestamp>2013-04-10T20:10:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>protocol relative</comment>
      <origin>494652</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19446" sha1="jc253ljb6njrw9s01ra3xcbxt267sq0" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
    return s .. notes    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[//toolserver.org/~geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents, Notes)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents, Notes)
    end
    return text
end

return coordinates</text>
      <sha1>jc253ljb6njrw9s01ra3xcbxt267sq0</sha1>
    </revision>
    <revision>
      <id>494653</id>
      <parentid>494652</parentid>
      <timestamp>2013-06-05T04:42:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>move to labs</comment>
      <origin>494653</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19448" sha1="53zri09l5m3jp763r6oft7ihcdqzx2e" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
    return s .. notes    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[//tools.wmflabs.org/geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents, Notes)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents, Notes)
    end
    return text
end

return coordinates</text>
      <sha1>53zri09l5m3jp763r6oft7ihcdqzx2e</sha1>
    </revision>
    <revision>
      <id>494654</id>
      <parentid>494653</parentid>
      <timestamp>2013-06-29T23:17:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;WOSlinker</username>
        <id>0</id>
      </contributor>
      <comment>trim spaces on direction for convert_dms2dec</comment>
      <origin>494654</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19516" sha1="40d0hehqw6bmg6p9wcljn8nzd342f7t" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

]]

math_mod = require( "Module:Math" );
globalFrame = nil

coordinates = {};

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
    return s .. notes    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. globalFrame:preprocess(
        '[//tools.wmflabs.org/geohack/geohack.php?pagename={{FULLPAGENAMEE}}&amp;params=' ..
        uriComponents .. ' ' .. inner .. ']') .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    direction = mw.ustring.gsub(direction, '^[ ]*(.-)[ ]*$', '%1');
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents, Notes)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents, Notes)
    end
    return text
end

return coordinates</text>
      <sha1>40d0hehqw6bmg6p9wcljn8nzd342f7t</sha1>
    </revision>
    <revision>
      <id>494655</id>
      <parentid>494654</parentid>
      <timestamp>2013-08-28T15:48:31Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dragons flight</username>
        <id>0</id>
      </contributor>
      <comment>update from sandbox.  Add export hook for coord_url, remove uneccesary use of frame:preprocess</comment>
      <origin>494655</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19856" sha1="32imxi13azn2etu9v4hviv4ob6ce203" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

math_mod = require( "Module:Math" );
globalFrame = nil
coordinates = {};

page_name = mw.uri.encode( mw.title.getCurrentTitle().prefixedText, 'WIKI' );
coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
    return s .. notes    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
        '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor
    direction = mw.ustring.gsub(direction, '^[ ]*(.-)[ ]*$', '%1');
    if direction == "N" or direction == "E" then
        factor = 1
    else
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
    {{ Invoke:Coordinates | link }}
    
]]
function coordinates.link(frame)
    return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents, Notes)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents, Notes)
    end
    return text
end

return coordinates</text>
      <sha1>32imxi13azn2etu9v4hviv4ob6ce203</sha1>
    </revision>
    <revision>
      <id>494656</id>
      <parentid>494655</parentid>
      <timestamp>2013-10-22T08:17:47Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <comment>change requested by [[User:Hike395]] to fix default hemisphere</comment>
      <origin>494656</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19832" sha1="byyab97dhagk3mctuvr93swk3pjpga8" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

math_mod = require( "Module:Math" );
globalFrame = nil
coordinates = {};

page_name = mw.uri.encode( mw.title.getCurrentTitle().prefixedText, 'WIKI' );
coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
    local l = "[[Geographic coordinate system|Coordinates]]: " .. s
    local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
    return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
    return s .. notes    
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
    local concatenated = first:upper() .. second:upper();
    
    if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
        concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
        return true;
    end
    return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
    local coordinateSpec = {}
    local errors = {}
    
    if long == "" or long == nil then
        return nil, {{"parseDec", "Missing longitude"}}
    end
    
    errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );    
    coordinateSpec["dec-lat"]  = lat;
    coordinateSpec["dec-long"] = long;

    local mode = coordinates.determineMode( lat, long );
    coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
    coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}    
    
    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dec"
    end

    return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
    if arg ~= nil and arg ~= "" then
        return arg .. suplement
    end
    return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
    local coordinateSpec = {}
    local errors = {}
    
    lat_f = lat_f:upper();
    long_f = long_f:upper();
    
    -- Check if specified backward
    if lat_f == 'E' or lat_f == 'W' then
        local t_d, t_m, t_s, t_f;
        t_d = lat_d;
        t_m = lat_m;
        t_s = lat_s;
        t_f = lat_f;
        lat_d = long_d;
        lat_m = long_m;
        lat_s = long_s;
        lat_f = long_f;
        long_d = t_d;
        long_m = t_m;
        long_s = t_s;
        long_f = t_f;
    end    
    
    errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
    if long_d == nil or long_d == "" then
        table.insert(errors, {"parseDMS", "Missing longitude" })
    end
    
    if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
        if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
            if lat_f:upper() == 'S' then 
                lat_d = '-' .. lat_d;
            end
            if long_f:upper() == 'W' then 
                long_d = '-' .. long_d;
            end     
            
            return parseDec( lat_d, long_d, format );
        end        
    end   
    
    coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
    coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
    coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
    coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

    if format ~= "" then
        coordinateSpec.default = format
    else
        coordinateSpec.default = "dms"
    end   

    return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
    local uriComponents = coordinateSpec["param"]
    if uriComponents == "" then
        -- RETURN error, should never be empty or nil
        return "ERROR param was empty"
    end
    if args["name"] ~= "" and args["name"] ~= nil then
        uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
    end
    
    local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
             .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
             .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
             .. '&lt;/span&gt;'

    local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
    if lat &lt; 0 then
        -- FIXME this breaks the pre-existing precision
        geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
    else
        geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
    end

    local long = tonumber( coordinateSpec["dec-long"] ) or 0
    if long &lt; 0 then
        -- FIXME does not handle unicode minus
        geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
    else
        geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
    end
    
    local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
             .. geodeclat .. ' '
             .. geodeclong
             .. '&lt;/span&gt;'

    local geonumhtml = '&lt;span class="geo"&gt;'
             .. coordinateSpec["dec-lat"] .. '; '
             .. coordinateSpec["dec-long"]
             .. '&lt;/span&gt;'

    local inner;
    inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
                .. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
                .. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

    if args["name"] == "" or args["name"] == nil then
        inner = inner .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
    else
        inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
                .. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
                .. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
    end

    return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
        '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
    local result = ""
    for i,v in ipairs(errors) do
        local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
        result = result .. errorHTML .. "&lt;br /&gt;"
    end
    return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
    if default == "" then
        default = "dec"
    end
    
    if default == mode then
        return "geo-default"
    else
        return "geo-nondefault"
    end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
    local result, errors;
    local primary = false;
    
    if args[1] == "" then
        -- no lat logic
        return errorPrinter( {{"formatTest", "Missing latitude"}} )
    elseif args[4] == "" and args[5] == "" and args[6] == "" then
        -- dec logic
        result, errors = parseDec( args[1], args[2], args['format'] )
        if result == nil then
            return errorPrinter( errors );
        end              
        result.param    = table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
    elseif dmsTest(args[4], args[8]) then
        -- dms logic
        result, errors = parseDMS( args[1], args[2], args[3], args[4], 
            args[5], args[6], args[7], args[8], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7], args[8], args[9] } , '_' );
        if args[10] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[3], args[6]) then
        -- dm logic
        result, errors = parseDMS( args[1], args[2], nil, args[3], 
            args[4], args[5], nil, args[6], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
            args[6], args[7] } , '_' );
        if args[8] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    elseif dmsTest(args[2], args[4]) then
        -- d logic
        result, errors = parseDMS( args[1], nil, nil, args[2], 
            args[3], nil, nil, args[4], args['format'] )
        result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
        if args[6] ~= '' then
            table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
        end        
    else
        -- Error
        return errorPrinter( {{"formatTest", "Unknown argument format"}} )
    end
    result.name     = args["name"]
    
    local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
    for _, v in ipairs( extra_param ) do
        if (args[v] or '') ~= '' then 
            table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
        end
    end
    
    if #errors == 0 then
        return specPrinter( args, result )    
    else
        return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
    end    
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
    local coord = tonumber(coordinate) or 0
    local postfix
    if coord &gt;= 0 then
        postfix = firstPostfix
    else
        postfix = secondPostfix
    end

    precision = precision:lower();
    if precision == "dms" then
        return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
    elseif precision == "dm" then
        return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
    elseif precision == "d" then
        return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
    end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
    local d = math_mod._round( coordinate, 0 ) .. "°"
    return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)    
    coordinate = math_mod._round( coordinate * 60, 0 );
    local m = coordinate % 60;
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"
    
    return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
    coordinate = math_mod._round( coordinate * 60 * 60, 0 );
    local s = coordinate % 60
    coordinate = math.floor( (coordinate - s) / 60 );
    local m = coordinate % 60
    coordinate = math.floor( (coordinate - m) / 60 );
    local d = coordinate % 360 .."°"

    return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
    local degrees = tonumber(degrees_str) or 0
    local minutes = tonumber(minutes_str) or 0
    local seconds = tonumber(seconds_str) or 0
    
    local factor = 1
    direction = mw.ustring.gsub(direction, '^[ ]*(.-)[ ]*$', '%1');
    if direction == "S" or direction == "W" then
        factor = -1
    end
    
    local precision = 0
    if seconds_str ~= nil and seconds_str ~= '' then
        precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
    elseif minutes_str ~= nil and minutes_str ~= '' then
        precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
    else
        precision = math.max( math_mod._precision(degrees_str), 0 );
    end
    
    local decimal = factor * (degrees+(minutes+seconds/60)/60) 
    return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
    local errors = {};
    lat_d = tonumber( lat_d ) or 0;
    lat_m = tonumber( lat_m ) or 0;
    lat_s = tonumber( lat_s ) or 0;
    long_d = tonumber( long_d ) or 0;
    long_m = tonumber( long_m ) or 0;
    long_s = tonumber( long_s ) or 0;

    if strong then
        if lat_d &lt; 0 then
            table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
        end
        if long_d &lt; 0 then
            table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
        end
        --[[ 
        #coordinates is inconsistent about whether this is an error.  If globe: is
        specified, it won't error on this condition, but otherwise it will.
        
        For not simply disable this check.
        
        if long_d &gt; 180 then
            table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
        end
        ]]
    end    
        
    if lat_d &gt; 90 then
        table.insert(errors, {source, "latitude degrees &gt; 90"})
    end
    if lat_d &lt; -90 then
        table.insert(errors, {source, "latitude degrees &lt; -90"})
    end
    if lat_m &gt;= 60 then
        table.insert(errors, {source, "latitude minutes &gt;= 60"})
    end
    if lat_m &lt; 0 then
        table.insert(errors, {source, "latitude minutes &lt; 0"})
    end
    if lat_s &gt;= 60 then
        table.insert(errors, {source, "latitude seconds &gt;= 60"})
    end
    if lat_s &lt; 0 then
        table.insert(errors, {source, "latitude seconds &lt; 0"})
    end
    if long_d &gt;= 360 then
        table.insert(errors, {source, "longitude degrees &gt;= 360"})
    end
    if long_d &lt;= -360 then
        table.insert(errors, {source, "longitude degrees &lt;= -360"})
    end
    if long_m &gt;= 60 then
        table.insert(errors, {source, "longitude minutes &gt;= 60"})
    end
    if long_m &lt; 0 then
        table.insert(errors, {source, "longitude minutes &lt; 0"})
    end
    if long_s &gt;= 60 then
        table.insert(errors, {source, "longitude seconds &gt;= 60"})
    end
    if long_s &lt; 0 then
        table.insert(errors, {source, "longitude seconds &lt; 0"})
    end
    
    return errors;
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
    {{ Invoke:Coordinates | link }}
    
]]
function coordinates.link(frame)
    return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
    {{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
        negative_suffix | precision }}
    
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
    globalFrame = frame
    local coordinate = frame.args[1]
    local firstPostfix = frame.args[2]
    local secondPostfix = frame.args[3]
    local precision = frame.args[4]

    return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
    local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
    if precision &lt;= 0 then
        return 'd'
    elseif precision &lt;= 2 then
        return 'dm';
    else
        return 'dms';
    end
end        

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
    {{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
        minutes | seconds }}
    
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
    globalFrame = frame
    local direction = frame.args[1]
    local degrees = frame.args[2]
    local minutes = frame.args[3]
    local seconds = frame.args[4]

    return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
    {{ Invoke:Coordinates | coord }}
    {{ Invoke:Coordinates | coord | lat | long }}
    {{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
    ...
    
    Refer to {{coord}} documentation page for many additional parameters and 
    configuration options.
    
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
    globalFrame = frame
    local args = frame.args
    if args[1] == nil then
        local pFrame = frame:getParent();
        args = pFrame.args;
        for k,v in pairs( frame.args ) do
            args[k] = v;
        end
    end
    
    for i=1,10 do 
        if args[i] == nil then 
            args[i] = ""
        else
            args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
        end        
    end
    args['format'] = args['format'] or '';
    
    local contents = formatTest(args)
    local Notes = args.notes or ""
    local Display = string.lower(args.display or "inline")
    if Display == '' then
        Display = 'inline';
    end
    
    local text = ''
    if string.find( Display, 'inline' ) ~= nil or Display == 'i' or 
            Display == 'it' or Display == 'ti' then
        text = displayinline(contents, Notes)
    end
    if string.find( Display, 'title' ) ~= nil or Display == 't' or 
            Display == 'it' or Display == 'ti' then
        text = text .. displaytitle(contents, Notes)
    end
    return text
end

return coordinates</text>
      <sha1>byyab97dhagk3mctuvr93swk3pjpga8</sha1>
    </revision>
    <revision>
      <id>494657</id>
      <parentid>494656</parentid>
      <timestamp>2013-11-22T03:35:56Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>add Wikidata tracking categories per protected edit request by [[User:Multichill]] at [[Template talk:Coord#Wikidata tracking]]; also convert whitespace to tabs</comment>
      <origin>494657</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18949" sha1="cd58d4l54jonvvm52w9haep7kyf9fqi" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

math_mod = require( "Module:Math" );
globalFrame = nil
coordinates = {};

current_page = mw.title.getCurrentTitle()
page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		table.insert(errors, {"parseDMS", "Missing longitude" })
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner;
	inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate) or 0
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str) or 0
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = mw.ustring.gsub(direction, '^[ ]*(.-)[ ]*$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	if args[1] == nil then
		local pFrame = frame:getParent();
		args = pFrame.args;
		for k,v in pairs( frame.args ) do
			args[k] = v;
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	local valid_Display = false
	if string.find( Display, 'inline' ) ~= nil or Display == 'i' or 
			Display == 'it' or Display == 'ti' then
		text = displayinline(contents, Notes)
		valid_Display = true
	end
	if string.find( Display, 'title' ) ~= nil or Display == 't' or 
			Display == 'it' or Display == 'ti' then
		text = text .. displaytitle(contents, Notes)
		valid_Display = true
	end
	local wikidata_cat
	if valid_Display and mw.wikibase and current_page.namespace == 0 then -- add the appropriate Wikidata category for main namespace pages
		local entity = mw.wikibase.getEntity()
		if entity and entity.claims.p625 then
			local snaktype = entity.claims.p625[0].mainsnak.snaktype
			if snaktype == 'value' then
				wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
			elseif snaktype == 'somevalue' then
				wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
			elseif snaktype == 'novalue' then
				wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
			end
		else
			wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
		end
	end
	wikidata_cat = wikidata_cat or ''
	text = text .. wikidata_cat
	return text
end

return coordinates</text>
      <sha1>cd58d4l54jonvvm52w9haep7kyf9fqi</sha1>
    </revision>
    <revision>
      <id>494658</id>
      <parentid>494657</parentid>
      <timestamp>2013-11-22T08:51:28Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>check for nil values of entity.claims per protected edit request by [[User:Hydrox]]</comment>
      <origin>494658</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18967" sha1="c0642xbkxssexlircfeih2r5jkjr0hm" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

math_mod = require( "Module:Math" );
globalFrame = nil
coordinates = {};

current_page = mw.title.getCurrentTitle()
page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		table.insert(errors, {"parseDMS", "Missing longitude" })
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner;
	inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate) or 0
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str) or 0
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = mw.ustring.gsub(direction, '^[ ]*(.-)[ ]*$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	if args[1] == nil then
		local pFrame = frame:getParent();
		args = pFrame.args;
		for k,v in pairs( frame.args ) do
			args[k] = v;
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	local valid_Display = false
	if string.find( Display, 'inline' ) ~= nil or Display == 'i' or 
			Display == 'it' or Display == 'ti' then
		text = displayinline(contents, Notes)
		valid_Display = true
	end
	if string.find( Display, 'title' ) ~= nil or Display == 't' or 
			Display == 'it' or Display == 'ti' then
		text = text .. displaytitle(contents, Notes)
		valid_Display = true
	end
	local wikidata_cat
	if valid_Display and mw.wikibase and current_page.namespace == 0 then -- add the appropriate Wikidata category for main namespace pages
		local entity = mw.wikibase.getEntity()
		if entity and entity.claims and entity.claims.p625 then
			local snaktype = entity.claims.p625[0].mainsnak.snaktype
			if snaktype == 'value' then
				wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
			elseif snaktype == 'somevalue' then
				wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
			elseif snaktype == 'novalue' then
				wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
			end
		else
			wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
		end
	end
	wikidata_cat = wikidata_cat or ''
	text = text .. wikidata_cat
	return text
end

return coordinates</text>
      <sha1>c0642xbkxssexlircfeih2r5jkjr0hm</sha1>
    </revision>
    <revision>
      <id>494659</id>
      <parentid>494658</parentid>
      <timestamp>2013-12-24T00:11:21Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>only add Wikidata categories if coordinates are displayed in the title</comment>
      <origin>494659</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="18966" sha1="2lu9r3zg0hkows0qaady16c1a6nke59" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

math_mod = require( "Module:Math" );
globalFrame = nil
coordinates = {};

current_page = mw.title.getCurrentTitle()
page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local dmsTest = function(first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[ Helper function, handle optional args. ]]
function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		table.insert(errors, {"parseDMS", "Missing longitude" })
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner;
	inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[
Formats any error messages generated for display
]]
function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate) or 0
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[ Helper function, convert decimal to degrees ]]
function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str) or 0
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = mw.ustring.gsub(direction, '^[ ]*(.-)[ ]*$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	if args[1] == nil then
		local pFrame = frame:getParent();
		args = pFrame.args;
		for k,v in pairs( frame.args ) do
			args[k] = v;
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
			local entity = mw.wikibase.getEntity()
			if entity and entity.claims and entity.claims.p625 then
				local snaktype = entity.claims.p625[0].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>2lu9r3zg0hkows0qaady16c1a6nke59</sha1>
    </revision>
    <revision>
      <id>494660</id>
      <parentid>494659</parentid>
      <timestamp>2014-06-26T04:14:32Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>avoid using global variables, and re-order the functions, per protected edit request by [[User:Jackmcbarn]]</comment>
      <origin>494660</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19118" sha1="ik27aqhbtg8grm28vjfa6qrf6q3uv5z" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require( "Module:Math" );
local globalFrame = nil
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate) or 0
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str) or 0
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = mw.ustring.gsub(direction, '^[ ]*(.-)[ ]*$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		table.insert(errors, {"parseDMS", "Missing longitude" })
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	if args[1] == nil then
		local pFrame = frame:getParent();
		args = pFrame.args;
		for k,v in pairs( frame.args ) do
			args[k] = v;
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
			local entity = mw.wikibase.getEntity()
			if entity and entity.claims and entity.claims.p625 then
				local snaktype = entity.claims.p625[0].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>ik27aqhbtg8grm28vjfa6qrf6q3uv5z</sha1>
    </revision>
    <revision>
      <id>494661</id>
      <parentid>494660</parentid>
      <timestamp>2014-07-13T10:09:28Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>simplify whitespace-trimming pattern per protected edit request by [[User:Jackmcbarn]]</comment>
      <origin>494661</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19102" sha1="dr3ytvga0vbq5njhsrn19xj9r5tgv5e" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require( "Module:Math" );
local globalFrame = nil
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate) or 0
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str) or 0
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = direction:gsub('^ *(.-) *$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		table.insert(errors, {"parseDMS", "Missing longitude" })
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end		
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	if args[1] == nil then
		local pFrame = frame:getParent();
		args = pFrame.args;
		for k,v in pairs( frame.args ) do
			args[k] = v;
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
			local entity = mw.wikibase.getEntity()
			if entity and entity.claims and entity.claims.p625 then
				local snaktype = entity.claims.p625[0].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>dr3ytvga0vbq5njhsrn19xj9r5tgv5e</sha1>
    </revision>
    <revision>
      <id>494662</id>
      <parentid>494661</parentid>
      <timestamp>2014-07-17T00:47:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>improve error handling per protected edit request by [[User:Jackmcbarn]]</comment>
      <origin>494662</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19665" sha1="gyo7r6ox06zhcene34293kat3z4zl2b" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require( "Module:Math" );
local globalFrame = nil
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate) or 0
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str) or 0
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = direction:gsub('^ *(.-) *$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end	
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	if args[1] == nil then
		local pFrame = frame:getParent();
		args = pFrame.args;
		for k,v in pairs( frame.args ) do
			args[k] = v;
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
			local entity = mw.wikibase.getEntity()
			if entity and entity.claims and entity.claims.p625 then
				local snaktype = entity.claims.p625[0].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>gyo7r6ox06zhcene34293kat3z4zl2b</sha1>
    </revision>
    <revision>
      <id>494663</id>
      <parentid>494662</parentid>
      <timestamp>2014-08-12T02:39:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Changed protection level of Module:Coordinates: [[WP:High-risk templates|High-risk Lua module]]: allow template editors ([Edit=Allow only template editors and admins] (indefinite) [Move=Allow only template editors and admins] (indefinite))</comment>
      <origin>494662</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19665" sha1="gyo7r6ox06zhcene34293kat3z4zl2b" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require( "Module:Math" );
local globalFrame = nil
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate) or 0
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str) or 0
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = direction:gsub('^ *(.-) *$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end	
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	if args[1] == nil then
		local pFrame = frame:getParent();
		args = pFrame.args;
		for k,v in pairs( frame.args ) do
			args[k] = v;
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
			local entity = mw.wikibase.getEntity()
			if entity and entity.claims and entity.claims.p625 then
				local snaktype = entity.claims.p625[0].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>gyo7r6ox06zhcene34293kat3z4zl2b</sha1>
    </revision>
    <revision>
      <id>494664</id>
      <parentid>494663</parentid>
      <timestamp>2014-08-12T02:54:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jackmcbarn</username>
        <id>0</id>
      </contributor>
      <comment>prevent blank degrees from being treated as 0</comment>
      <origin>494664</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19655" sha1="irlsil3nb1zlmarskhy7q2z6ns8kzns" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require( "Module:Math" );
local globalFrame = nil
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = direction:gsub('^ *(.-) *$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end	
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	if args[1] == nil then
		local pFrame = frame:getParent();
		args = pFrame.args;
		for k,v in pairs( frame.args ) do
			args[k] = v;
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = args[i]:match( '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
			local entity = mw.wikibase.getEntity()
			if entity and entity.claims and entity.claims.p625 then
				local snaktype = entity.claims.p625[0].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>irlsil3nb1zlmarskhy7q2z6ns8kzns</sha1>
    </revision>
    <revision>
      <id>494665</id>
      <parentid>494664</parentid>
      <timestamp>2014-09-10T10:57:28Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <comment>import coordinates from WikiData, per discussion</comment>
      <origin>494665</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20122" sha1="bx97eo5upd15ijgkpr3zpj0epq46inu" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require( "Module:Math" );
local globalFrame = nil
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = direction:gsub('^ *(.-) *$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end	
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	local pFrame = frame:getParent();
	if args[1] == nil then
		args = pFrame.args;
		if pFrame.args[1] ~= nil then
			for k,v in pairs( frame.args ) do
				args[k] = v;
			end
		elseif pFrame.args[1] == nil and mw.wikibase.getEntityObject() ~= nil then
			local entity = mw.wikibase.entity.create( mw.wikibase.getEntityObject() )
			if entity and entity.claims and entity.claims.P625 and 
			entity.claims.P625[1].mainsnak.snaktype == "value" then
				args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
				args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			end
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = string.match(args[i], '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
			local entity = mw.wikibase.getEntity()
			if entity and entity.claims and entity.claims.p625 then
				local snaktype = entity.claims.p625[0].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>bx97eo5upd15ijgkpr3zpj0epq46inu</sha1>
    </revision>
    <revision>
      <id>494666</id>
      <parentid>494665</parentid>
      <timestamp>2015-03-03T14:23:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hoo man</username>
        <id>0</id>
      </contributor>
      <comment>Fulfill edit request, remove superfluous function call</comment>
      <origin>494666</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20169" sha1="q63rcn9pethnk8gj7mqjzfzkxibmd2r" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require( "Module:Math" );
local globalFrame = nil
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = direction:gsub('^ *(.-) *$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end	
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	local pFrame = frame:getParent();
	if args[1] == nil then
		args = pFrame.args;
		if pFrame.args[1] ~= nil then
			for k,v in pairs( frame.args ) do
				args[k] = v;
			end
		elseif pFrame.args[1] == nil and mw.wikibase.getEntityObject() ~= nil then
			local entity = mw.wikibase.getEntityObject()
			if entity and entity.claims and entity.claims.P625 and 
			entity.claims.P625[1].mainsnak.snaktype == "value" then
				args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
				args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			end
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = string.match(args[i], '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
                        local entity = mw.wikibase.getEntityObject()
                        if entity and entity.claims and entity.claims.P625 then
                                local snaktype = entity.claims.P625[1].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>q63rcn9pethnk8gj7mqjzfzkxibmd2r</sha1>
    </revision>
    <revision>
      <id>494667</id>
      <parentid>494666</parentid>
      <timestamp>2015-03-12T14:26:18Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>responding to edit request, please revert if this causes a problem</comment>
      <origin>494667</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19524" sha1="1cfudyojc2chwmlzywmlzabhgj3z7mg" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local title = mw.html.create('span')
	title
		:attr('id', 'coordinates')
		:css('font-size', 'small')
		:wikitext('[[Geographic coordinate system|Coordinates]]: ' .. s .. notes)
	return tostring(title)
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 then
			local snaktype = entity.claims.P625[0].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

return coordinates</text>
      <sha1>1cfudyojc2chwmlzywmlzabhgj3z7mg</sha1>
    </revision>
    <revision>
      <id>494668</id>
      <parentid>494667</parentid>
      <timestamp>2015-03-12T14:33:50Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>fix nil error</comment>
      <origin>494668</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19550" sha1="m5ix0xif664j7e8fh7ch5umxdcgqn7g" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local title = mw.html.create('span')
	title
		:attr('id', 'coordinates')
		:css('font-size', 'small')
		:wikitext('[[Geographic coordinate system|Coordinates]]: ' .. s .. notes)
	return tostring(title)
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[0] then
			local snaktype = entity.claims.P625[0].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

return coordinates</text>
      <sha1>m5ix0xif664j7e8fh7ch5umxdcgqn7g</sha1>
    </revision>
    <revision>
      <id>494669</id>
      <parentid>494668</parentid>
      <timestamp>2015-03-12T14:39:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>merge prior edit</comment>
      <origin>494669</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19550" sha1="169d74podb1gfkri87lepc7twip2gq4" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local title = mw.html.create('span')
	title
		:attr('id', 'coordinates')
		:css('font-size', 'small')
		:wikitext('[[Geographic coordinate system|Coordinates]]: ' .. s .. notes)
	return tostring(title)
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

return coordinates</text>
      <sha1>169d74podb1gfkri87lepc7twip2gq4</sha1>
    </revision>
    <revision>
      <id>494670</id>
      <parentid>494669</parentid>
      <timestamp>2015-03-12T15:50:58Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>rv for now</comment>
      <origin>494670</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="20169" sha1="q63rcn9pethnk8gj7mqjzfzkxibmd2r" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require( "Module:Math" );
local globalFrame = nil
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline (s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	local concatenated = first:upper() .. second:upper();
	
	if concatenated == "NE" or concatenated == "NW" or concatenated == "SE" or concatenated == "SW" or
		concatenated == "EN" or concatenated == "WN" or concatenated == "ES" or concatenated == "WS" then
		return true;
	end
	return false;
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, suplement)
	if arg ~= nil and arg ~= "" then
		return arg .. suplement
	end
	return ""
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] ~= "" and args["name"] ~= nil then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = coordinateSpec["dec-lat"]:sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = coordinateSpec["dec-long"]:sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if args["name"] == "" or args["name"] == nil then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	direction = direction:gsub('^ *(.-) *$', '%1');
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str ~= nil and seconds_str ~= '' then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str ~= nil and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if long == "" or long == nil then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if long_d == nil or long_d == "" then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if lat_m == nil and lat_s == nil and long_m == nil and long_s == nil and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format ~= "" then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors;
	local primary = false;
	
	if args[1] == "" then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif args[4] == "" and args[5] == "" and args[6] == "" then
		-- dec logic
		result, errors = parseDec( args[1], args[2], args['format'] )
		if result == nil then
			return errorPrinter( errors );
		end			  
		result.param	= table.concat( {args[1], "_N_", args[2], "_E_", args[3] } );
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS( args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args['format'] )
		if args[10] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7], args[8], args[9] } , '_' );
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS( args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'] )
		if args[8] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5],
			args[6], args[7] } , '_' );
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS( args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args['format'] )
		if args[6] ~= '' then
			table.insert( errors, { 'formatTest', 'Extra unexpected parameters' } );
		end	
		if result == nil then
			return errorPrinter( errors );
		end
		result.param = table.concat( { args[1], args[2], args[3], args[4], args[5] } , '_' );
	else
		-- Error
		return errorPrinter( {{"formatTest", "Unknown argument format"}} )
	end
	result.name	 = args["name"]
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs( extra_param ) do
		if (args[v] or '') ~= '' then 
			table.insert( errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' } );
		end
	end
	
	if #errors == 0 then
		return specPrinter( args, result )	
	else
		return specPrinter( args, result ) .. " " .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'; 
	end	
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
function coordinates.dec2dms(frame)
	globalFrame = frame
	local coordinate = frame.args[1]
	local firstPostfix = frame.args[2]
	local secondPostfix = frame.args[3]
	local precision = frame.args[4]

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
function coordinates.dms2dec(frame)
	globalFrame = frame
	local direction = frame.args[1]
	local degrees = frame.args[2]
	local minutes = frame.args[3]
	local seconds = frame.args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
function coordinates.coord(frame)
	globalFrame = frame
	local args = frame.args
	local pFrame = frame:getParent();
	if args[1] == nil then
		args = pFrame.args;
		if pFrame.args[1] ~= nil then
			for k,v in pairs( frame.args ) do
				args[k] = v;
			end
		elseif pFrame.args[1] == nil and mw.wikibase.getEntityObject() ~= nil then
			local entity = mw.wikibase.getEntityObject()
			if entity and entity.claims and entity.claims.P625 and 
			entity.claims.P625[1].mainsnak.snaktype == "value" then
				args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
				args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			end
		end
	end
	
	for i=1,10 do 
		if args[i] == nil then 
			args[i] = ""
		else
			args[i] = string.match(args[i], '^%s*(.-)%s*$' );  --remove whitespace
		end		
	end
	args['format'] = args['format'] or '';
	
	local contents = formatTest(args)
	local Notes = args.notes or ""
	local Display = string.lower(args.display or "inline")
	if Display == '' then
		Display = 'inline';
	end
	
	local text = ''
	if string.find( Display, 'inline' ) ~= nil
		or Display == 'i'
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed inline.
		text = displayinline(contents, Notes)
	end
	if string.find( Display, 'title' ) ~= nil
		or Display == 't' 
		or Display == 'it'
		or Display == 'ti'
	then
		-- Coordinates are displayed in the title.
		text = text .. displaytitle(contents, Notes)
		-- Add a Wikidata category for mainspace pages.
		if mw.wikibase and current_page.namespace == 0 then
			local wikidata_cat
                        local entity = mw.wikibase.getEntityObject()
                        if entity and entity.claims and entity.claims.P625 then
                                local snaktype = entity.claims.P625[1].mainsnak.snaktype
				if snaktype == 'value' then
					wikidata_cat = '[[Category:Coordinates on Wikidata]]' -- coordinates exist both here and on Wikidata, and can be compared
				elseif snaktype == 'somevalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to unknown value]]'
				elseif snaktype == 'novalue' then
					wikidata_cat = '[[Category:Coordinates on Wikidata set to no value]]'
				end
			else
				wikidata_cat = '[[Category:Coordinates not on Wikidata]]' -- we have to either import the coordinates to Wikidata or remove them here
			end
			if wikidata_cat then
				text = text .. wikidata_cat
			end
		end
	end

	return text
end

return coordinates</text>
      <sha1>q63rcn9pethnk8gj7mqjzfzkxibmd2r</sha1>
    </revision>
    <revision>
      <id>494671</id>
      <parentid>494670</parentid>
      <timestamp>2015-03-12T15:53:32Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>remerge changes, but without changes to displaytitle</comment>
      <origin>494671</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19542" sha1="21lzppc6u3ihi5onxz06jrzotwn01di" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle (s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest (first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

return coordinates</text>
      <sha1>21lzppc6u3ihi5onxz06jrzotwn01di</sha1>
    </revision>
    <revision>
      <id>494672</id>
      <parentid>494671</parentid>
      <timestamp>2015-03-18T14:53:14Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>per edit request on talk page</comment>
      <origin>494672</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="19803" sha1="o7oa65vk5twc3i85sono49wsf5kop0g" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#Invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#Invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#Invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#Invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{ Invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{ Invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{ Invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{ Invoke:Coordinates | coord }}
	{{ Invoke:Coordinates | coord | lat | long }}
	{{ Invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

return coordinates</text>
      <sha1>o7oa65vk5twc3i85sono49wsf5kop0g</sha1>
    </revision>
    <revision>
      <id>494673</id>
      <parentid>494672</parentid>
      <timestamp>2016-08-31T09:33:49Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <comment>add new functions per request of [[User:Jc86035]]</comment>
      <origin>494673</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="21639" sha1="hmaceynn8xe6j2ep8fvumc1x2f2n94m" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ', 1) end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>hmaceynn8xe6j2ep8fvumc1x2f2n94m</sha1>
    </revision>
    <revision>
      <id>494674</id>
      <parentid>494673</parentid>
      <timestamp>2016-09-13T15:45:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Andy M. Wang</username>
        <id>0</id>
      </contributor>
      <comment>sandbox sync per [[Special:Permalink/739223562#TPE on 11 Sep 2016|TPER at talk]], bring #coordinates to module</comment>
      <origin>494674</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="22581" sha1="a1f0n21asbd1jf79xvmpv4lnr9iwkox" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(frame, in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInline(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(frame, args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ') end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>a1f0n21asbd1jf79xvmpv4lnr9iwkox</sha1>
    </revision>
    <revision>
      <id>494675</id>
      <parentid>494674</parentid>
      <timestamp>2016-09-13T16:02:23Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Andy M. Wang</username>
        <id>0</id>
      </contributor>
      <comment>mal-synced? backout per [[Special:Diff/739241079/739244877|issue noted]] at [[Grade I listed buildings in Dorset]], getting "cannot have more than one primary tag per page"</comment>
      <origin>494675</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="21639" sha1="hmaceynn8xe6j2ep8fvumc1x2f2n94m" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ', 1) end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>hmaceynn8xe6j2ep8fvumc1x2f2n94m</sha1>
    </revision>
    <revision>
      <id>494676</id>
      <parentid>494675</parentid>
      <timestamp>2016-09-13T16:19:42Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Andy M. Wang</username>
        <id>0</id>
      </contributor>
      <comment>sync again, typo fix</comment>
      <origin>494676</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="22582" sha1="41c7pn3ybtbw8vntmfjx73eii89mwfx" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(frame, in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(frame, args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ') end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>41c7pn3ybtbw8vntmfjx73eii89mwfx</sha1>
    </revision>
    <revision>
      <id>494677</id>
      <parentid>494676</parentid>
      <timestamp>2016-09-20T15:59:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Andy M. Wang</username>
        <id>0</id>
      </contributor>
      <comment>Undid revision 739248415 by [[Special:Contributions/Andy M. Wang|Andy M. Wang]] ([[User talk:Andy M. Wang|talk]]): compatibility issues remaining with [[Module:HS listed building]]</comment>
      <origin>494677</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="21639" sha1="hmaceynn8xe6j2ep8fvumc1x2f2n94m" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ', 1) end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>hmaceynn8xe6j2ep8fvumc1x2f2n94m</sha1>
    </revision>
    <revision>
      <id>494678</id>
      <parentid>494677</parentid>
      <timestamp>2016-09-22T04:52:28Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Andy M. Wang</username>
        <id>0</id>
      </contributor>
      <comment>re-sync; call coordinates parser function again; no other apparent compatibility issues with other modules</comment>
      <origin>494678</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="22598" sha1="tw6n4lajsdwdjzjlsx9gk8vtgj1wn3y" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ') end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>tw6n4lajsdwdjzjlsx9gk8vtgj1wn3y</sha1>
    </revision>
    <revision>
      <id>494679</id>
      <parentid>494678</parentid>
      <timestamp>2016-11-25T08:11:11Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jc86035</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>allowed geohack in first parameter for Wikidata</comment>
      <origin>494679</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="22660" sha1="o7qy0r11m1gjtxz94b9pliftdg7ca3u" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if (not args[1] or not tonumber(args[1])) and not args[2] and mw.wikibase.getEntityObject() then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ') end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>o7qy0r11m1gjtxz94b9pliftdg7ca3u</sha1>
    </revision>
    <revision>
      <id>494680</id>
      <parentid>494679</parentid>
      <timestamp>2017-01-26T18:51:36Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MaxSem</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>dummy edit to force a refresh</comment>
      <origin>494680</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="22659" sha1="fvkm8dsaqul1q1bgwf1sn8h4kskkj7e" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if (not args[1] or not tonumber(args[1])) and not args[2] and mw.wikibase.getEntityObject() then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ') end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>fvkm8dsaqul1q1bgwf1sn8h4kskkj7e</sha1>
    </revision>
    <revision>
      <id>494681</id>
      <parentid>494680</parentid>
      <timestamp>2017-01-30T16:33:52Z</timestamp>
      <contributor>
        <username>wikipedia&gt;JJMC89</username>
        <id>0</id>
      </contributor>
      <comment>coord2text: Allow lat/long to start with a decimal or a number</comment>
      <origin>494681</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="22657" sha1="kwt0cl7n13jj418wciplakpbxu4azz3" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if (not args[1] or not tonumber(args[1])) and not args[2] and mw.wikibase.getEntityObject() then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ') end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>kwt0cl7n13jj418wciplakpbxu4azz3</sha1>
    </revision>
    <revision>
      <id>494682</id>
      <parentid>494681</parentid>
      <timestamp>2017-07-05T14:15:52Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>optional |name= for coordinsert as discussed on the talk page</comment>
      <origin>494682</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23235" sha1="nz0v3c3ikp217ztz81uxd1zqbfxag8m" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if (not args[1] or not tonumber(args[1])) and not args[2] and mw.wikibase.getEntityObject() then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then 
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1], 
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)', 
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>nz0v3c3ikp217ztz81uxd1zqbfxag8m</sha1>
    </revision>
    <revision>
      <id>494683</id>
      <parentid>494682</parentid>
      <timestamp>2017-12-27T15:41:35Z</timestamp>
      <contributor>
        <username>wikipedia&gt;TheDJ</username>
        <id>0</id>
      </contributor>
      <comment>normalize decimal notation in geohack urls to positive values, for maximum compatibility with geohack, WMA and geodata</comment>
      <origin>494683</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23571" sha1="3a3ynlsqw3m8nfmohklo5oobbn2ot0z" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])), 
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N', 
			math.abs(tonumber(args[2])), 
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E', 
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if (not args[1] or not tonumber(args[1])) and not args[2] and mw.wikibase.getEntityObject() then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then 
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1], 
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)', 
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>3a3ynlsqw3m8nfmohklo5oobbn2ot0z</sha1>
    </revision>
    <revision>
      <id>494684</id>
      <parentid>494683</parentid>
      <timestamp>2018-11-16T14:38:43Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>add support for optional qid for [[template:infobox observatory]]</comment>
      <origin>494684</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23680" sha1="ro4ilkzf9iixta1amaqf5nno2uuanje" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])), 
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N', 
			math.abs(tonumber(args[2])), 
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E', 
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = qid and mw.wikibase.getEntityObject(qid) or mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if (not args[1] or not tonumber(args[1])) and not args[2] and mw.wikibase.getEntityObject() then
		args[3] = args[1]; args[1] = nil
		local entity = args.qid and mw.wikibase.getEntityObject(args.qid) or mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then 
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1], 
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)', 
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>ro4ilkzf9iixta1amaqf5nno2uuanje</sha1>
    </revision>
    <revision>
      <id>494685</id>
      <parentid>494684</parentid>
      <timestamp>2019-05-07T07:30:58Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>Limit error checking to main (article) space</comment>
      <origin>494685</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23739" sha1="q6m5lvyua9waekvnx5p13elw31piln2" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])), 
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N', 
			math.abs(tonumber(args[2])), 
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E', 
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		if mw.title.getCurrentTitle().namespace == 0 then
			ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
		end
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = qid and mw.wikibase.getEntityObject(qid) or mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if (not args[1] or not tonumber(args[1])) and not args[2] and mw.wikibase.getEntityObject() then
		args[3] = args[1]; args[1] = nil
		local entity = args.qid and mw.wikibase.getEntityObject(args.qid) or mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then 
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1], 
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)', 
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>q6m5lvyua9waekvnx5p13elw31piln2</sha1>
    </revision>
    <revision>
      <id>494686</id>
      <parentid>494685</parentid>
      <timestamp>2019-07-05T07:48:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Johnuniq</username>
        <id>0</id>
      </contributor>
      <comment>update from sandbox per [[Template talk:Coord#Support for qid]]: do not require current page to have an entity so qid=Qxxx will work on any page; the only pages in the category are due to [[mw:Extension:GeoData]] which cannot be controlled by the module</comment>
      <origin>494686</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23496" sha1="80b4ph5p6mpsmszbrt2obj5aszyds5q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' ..
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = qid and mw.wikibase.getEntityObject(qid) or mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>80b4ph5p6mpsmszbrt2obj5aszyds5q</sha1>
    </revision>
    <revision>
      <id>494687</id>
      <parentid>494686</parentid>
      <timestamp>2019-07-05T13:52:23Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>Restore May 7 changes by Jonesey95 mistakenly deleted by latest edit (due to lack of sandbox sync)</comment>
      <origin>494687</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23651" sha1="m908cn3bmdq7fkv9n6catmu4uv1e391" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors, backward
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])), 
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N', 
			math.abs(tonumber(args[2])), 
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E', 
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		if mw.title.getCurrentTitle().namespace == 0 then
			ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
		end
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = qid and mw.wikibase.getEntityObject(qid) or mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then 
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1], 
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)', 
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>m908cn3bmdq7fkv9n6catmu4uv1e391</sha1>
    </revision>
    <revision>
      <id>494688</id>
      <parentid>494687</parentid>
      <timestamp>2019-07-06T04:05:22Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>self-rv: Johnuniq wanted to remove namespace test for both category and error output. Also looks like he explicitly removed a bunch whitespace.</comment>
      <origin>494688</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23496" sha1="80b4ph5p6mpsmszbrt2obj5aszyds5q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' ..
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = qid and mw.wikibase.getEntityObject(qid) or mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>80b4ph5p6mpsmszbrt2obj5aszyds5q</sha1>
    </revision>
    <revision>
      <id>494689</id>
      <parentid>494688</parentid>
      <timestamp>2020-07-07T23:18:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>Update Geohack link per talk page request.</comment>
      <origin>494689</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23492" sha1="2q940l1tsd9xsx16lreatmj5aikc803" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' ..
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = qid and mw.wikibase.getEntityObject(qid) or mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>2q940l1tsd9xsx16lreatmj5aikc803</sha1>
    </revision>
    <revision>
      <id>494690</id>
      <parentid>494689</parentid>
      <timestamp>2020-12-16T21:19:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;WOSlinker</username>
        <id>0</id>
      </contributor>
      <comment>use templatestyles</comment>
      <origin>494690</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23665" sha1="9q5lxwv4g12m02nvqvghh97muf83lt1" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = qid and mw.wikibase.getEntityObject(qid) or mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>9q5lxwv4g12m02nvqvghh97muf83lt1</sha1>
    </revision>
    <revision>
      <id>494691</id>
      <parentid>494690</parentid>
      <timestamp>2021-06-04T19:27:39Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>per request</comment>
      <origin>494691</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23719" sha1="1pyjpau2u6uomp8o2jr1hc6vxskmp3j" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>1pyjpau2u6uomp8o2jr1hc6vxskmp3j</sha1>
    </revision>
    <revision>
      <id>494692</id>
      <parentid>494691</parentid>
      <timestamp>2021-08-12T18:15:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;WOSlinker</username>
        <id>0</id>
      </contributor>
      <comment>add cat to error as per edit request</comment>
      <origin>494692</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23774" sha1="l5u87lywsikgugh9sibilxqd29jo2jo" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>l5u87lywsikgugh9sibilxqd29jo2jo</sha1>
    </revision>
    <revision>
      <id>494693</id>
      <parentid>494692</parentid>
      <timestamp>2022-02-05T04:39:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>bite this bullet and make it an indicator; tested on Firefox and everything looked ok in Vector, Vector 22, and Timeless</comment>
      <origin>494693</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24075" sha1="3glvo8qdliyw4ak10hm2ffcv0nlmcrx" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local coords = s .. notes
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		-- we need to add mw-parser-output ourselves because of [[phab:T188443]]
		content = '&lt;span class="mw-parser-output"&gt;&lt;span id="coordinates" class="coords-display" style="font-size: smaller"&gt;' ..
		'[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;&lt;/span&gt;'
	} .. '&lt;span class="coords-for-coord2text" style="display: none"&gt;' .. coords .. '&lt;/span&gt;'
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>3glvo8qdliyw4ak10hm2ffcv0nlmcrx</sha1>
    </revision>
    <revision>
      <id>494694</id>
      <parentid>494693</parentid>
      <timestamp>2022-02-05T20:25:38Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>make its own span and put inside</comment>
      <origin>494694</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24081" sha1="5quve98n2q8y8xrg7vwfqu8blwwlslt" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local coords = s .. notes
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		-- we need to add mw-parser-output ourselves because of [[phab:T188443]]
		content = '&lt;span class="mw-parser-output"&gt;&lt;span class="coords-display" style="font-size: smaller"&gt;&lt;span id="coordinates"&gt;' ..
		'[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;&lt;/span&gt;'
	} .. '&lt;span class="coords-for-coord2text" style="display: none"&gt;' .. coords .. '&lt;/span&gt;'
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>5quve98n2q8y8xrg7vwfqu8blwwlslt</sha1>
    </revision>
    <revision>
      <id>494695</id>
      <parentid>494694</parentid>
      <timestamp>2022-02-05T20:26:29Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>and one more /span</comment>
      <origin>494695</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24088" sha1="dbwnuj78wrgxwnhw4y6coxyjeflj23r" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local coords = s .. notes
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		-- we need to add mw-parser-output ourselves because of [[phab:T188443]]
		content = '&lt;span class="mw-parser-output"&gt;&lt;span class="coords-display" style="font-size: smaller"&gt;&lt;span id="coordinates"&gt;' ..
		'[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	} .. '&lt;span class="coords-for-coord2text" style="display: none"&gt;' .. coords .. '&lt;/span&gt;'
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>dbwnuj78wrgxwnhw4y6coxyjeflj23r</sha1>
    </revision>
    <revision>
      <id>494696</id>
      <parentid>494695</parentid>
      <timestamp>2022-02-05T21:07:43Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>rv; between [[Module talk:Coordinates#coordinsert feature broken]] and [[MediaWiki talk:Vector-2022.css#Interface-protected edit request on 5 February 2022]]</comment>
      <origin>494696</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23774" sha1="l5u87lywsikgugh9sibilxqd29jo2jo" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>l5u87lywsikgugh9sibilxqd29jo2jo</sha1>
    </revision>
    <revision>
      <id>494697</id>
      <parentid>494696</parentid>
      <timestamp>2022-02-25T14:10:23Z</timestamp>
      <contributor>
        <username>Xaosflux</username>
        <id>25</id>
      </contributor>
      <minor/>
      <comment>Changed protection settings for "[[Module:Coordinates]]": 1MM+ uses ([Edit=Require administrator access] (indefinite) [Move=Require administrator access] (indefinite))</comment>
      <origin>494696</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23774" sha1="l5u87lywsikgugh9sibilxqd29jo2jo" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('Module:No globals')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>l5u87lywsikgugh9sibilxqd29jo2jo</sha1>
    </revision>
    <revision>
      <id>494698</id>
      <parentid>494697</parentid>
      <timestamp>2022-10-22T12:01:04Z</timestamp>
      <contributor>
        <username>wikipedia&gt;WOSlinker</username>
        <id>0</id>
      </contributor>
      <comment>use require('strict') instead of require('Module:No globals')</comment>
      <origin>494698</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="23763" sha1="eg95m4tyhfjhtqz8wnyebv793csuu79" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='
local templatestyles = 'Module:Coordinates/styles.css'

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return mw.getCurrentFrame():extensionTag{ name = 'templatestyles', args = { src = templatestyles} }
	       .. '&lt;span class="plainlinks nourlexpansion"&gt;'
	       .. '[' .. coord_link .. uriComponents .. ' ' .. inner .. ']'
	       .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3')
			frame.args[1] = mw.ustring.gsub(frame.args[1], '(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ', '%1&amp;title=' .. mw.uri.encode(namestr) .. ' ')
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>eg95m4tyhfjhtqz8wnyebv793csuu79</sha1>
    </revision>
    <revision>
      <id>494699</id>
      <parentid>494698</parentid>
      <timestamp>2023-05-21T09:44:31Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Dreamy Jazz</username>
        <id>0</id>
      </contributor>
      <comment>Use indicator for KML data - requested by [[User:TheDJ|TheDJ]] and [[User:Jon (WMF)|Jon]]</comment>
      <origin>494699</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24764" sha1="gzw4vnc75m2mddb3wbbh7fbgl7ofvtc" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local coords = s .. notes;

	return '&lt;!-- COORD INDICATOR START ' .. coords .. ' --&gt;'
			.. mw.getCurrentFrame():extensionTag{
				name = 'indicator',
				args = { name = 'coordinates' },
				content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
			}
			.. '&lt;!-- COORD INDICATOR END --&gt;'
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. contents .. Notes
	end
	if isInTitle(Display) then
		text = text .. displaytitle(contents, Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- find the in-source 'indicators' and then call displaytitle
	mw.ustring.gsub(
		frame.args[1],
		'&lt;!%-%- COORD INDICATOR START (.-) %-%-&gt;.-&lt;!%-%- COORD INDICATOR END %-%-&gt;',
		function (coords) return displaytitle(coords, '') end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>gzw4vnc75m2mddb3wbbh7fbgl7ofvtc</sha1>
    </revision>
    <revision>
      <id>494700</id>
      <parentid>494699</parentid>
      <timestamp>2023-05-24T21:19:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>per tper</comment>
      <origin>494700</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24791" sha1="px7zvxjl32v0d6dglgxcsnqh1pg2i08" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local coords = s .. notes;

	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. contents .. Notes
	end
	if isInTitle(Display) then
		text = text .. displaytitle(contents, Notes) .. makeWikidataCategories(args.qid)
		
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			return '&lt;div class="geo-inline-hidden"&gt;' .. contents .. Notes .. '&lt;/div&gt;'
		end
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- find the in-source 'indicators' and then call displaytitle
	mw.ustring.gsub(
		frame.args[1],
		'&lt;div class="geo-inline-hidden"&gt;(.+&lt;/span&gt;)(.+)&lt;/div&gt;',
		displaytitle
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>px7zvxjl32v0d6dglgxcsnqh1pg2i08</sha1>
    </revision>
    <revision>
      <id>494701</id>
      <parentid>494700</parentid>
      <timestamp>2023-05-24T22:28:34Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>per talk page</comment>
      <origin>494701</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24776" sha1="b8otnh33vi4ouysa6sys1cjajbu9jd5" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. contents .. Notes
	end
	if isInTitle(Display) then
		text = text .. displaytitle(contents .. Notes)
		
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;div class="geo-inline-hidden"&gt;' .. contents .. Notes .. '&lt;/div&gt;'
		end
		
		text = text .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- find the in-source 'indicators' and then call displaytitle
	mw.ustring.gsub(
		frame.args[1],
		'&lt;div class="geo-inline-hidden"&gt;(.+)&lt;/div&gt;',
		displaytitle
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>b8otnh33vi4ouysa6sys1cjajbu9jd5</sha1>
    </revision>
    <revision>
      <id>494702</id>
      <parentid>494701</parentid>
      <timestamp>2023-06-05T14:59:31Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <comment>make coordinsert work properly with the indicator in the title, coord2text callable from other modules</comment>
      <origin>494702</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24968" sha1="6kta5btgj92lwlrw5jt86ohy8l48jv5" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>6kta5btgj92lwlrw5jt86ohy8l48jv5</sha1>
    </revision>
    <revision>
      <id>494703</id>
      <parentid>494702</parentid>
      <timestamp>2023-06-06T16:50:52Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>add noexcerpt to see if that hack works</comment>
      <origin>494703</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24978" sha1="0ph50norja3al1solwynjmubfjzomze" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>0ph50norja3al1solwynjmubfjzomze</sha1>
    </revision>
    <revision>
      <id>494704</id>
      <parentid>494703</parentid>
      <timestamp>2023-06-21T17:33:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Ladsgroup</username>
        <id>0</id>
      </contributor>
      <comment>Switch from proto-relative urls (//foo.bar.com) to HTTPS (https://foo.bar.com). Since we are only serving https in Wikimedia now, it always outputs https links. At the same time, proto-relative add two duplicate rows to externallinks table meaning this is adding tens of millions of extra and useless rows bloating the table</comment>
      <origin>494704</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="24984" sha1="bdj0k7qj47pqjhdgq52hamp1sj9atib" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>bdj0k7qj47pqjhdgq52hamp1sj9atib</sha1>
    </revision>
    <revision>
      <id>494705</id>
      <parentid>494704</parentid>
      <timestamp>2023-12-27T16:54:38Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Add class and data per request</comment>
      <origin>494705</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25024" sha1="19ko3dcl6p5dgsrp2255w3zyviusr8s" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion load-gadget" data-gadget="WikiMiniAtlas"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>19ko3dcl6p5dgsrp2255w3zyviusr8s</sha1>
    </revision>
    <revision>
      <id>494534</id>
      <timestamp>2024-05-31T00:22:22Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Switch to template gadget category per request</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494535</id>
      <parentid>494534</parentid>
      <timestamp>2024-06-26T02:51:33Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:Coordinates]]</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494536</id>
      <parentid>494535</parentid>
      <timestamp>2025-02-27T21:13:08Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Switch to template gadget category per request</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494537</id>
      <parentid>494536</parentid>
      <timestamp>2025-02-27T21:13:09Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing all the coordinates module stuff with the timestamps removed in order to force updates</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494538</id>
      <parentid>494537</parentid>
      <timestamp>2025-02-27T21:15:49Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Switch to template gadget category per request</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494706</id>
      <parentid>494538</parentid>
      <timestamp>2025-03-18T09:19:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>167 revisions imported from [[:wikipedia:Module:Coordinates]]: Import just to get the source</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494707</id>
      <parentid>494706</parentid>
      <timestamp>2025-05-04T10:12:48Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Switch to template gadget category per request</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494708</id>
      <parentid>494707</parentid>
      <timestamp>2025-05-04T10:12:49Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494709</id>
      <parentid>494708</parentid>
      <timestamp>2025-07-07T21:44:18Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Switch to template gadget category per request</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494710</id>
      <parentid>494709</parentid>
      <timestamp>2025-07-07T22:04:36Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Switch to template gadget category per request</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494711</id>
      <parentid>494710</parentid>
      <timestamp>2025-07-07T22:04:37Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494712</id>
      <parentid>494711</parentid>
      <timestamp>2025-11-21T00:34:25Z</timestamp>
      <contributor>
        <username>en&gt;Ferien</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:en:Module:Coordinates]]</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494713</id>
      <parentid>494712</parentid>
      <timestamp>2025-11-21T00:34:26Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting coordinates so I hope they start working</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494714</id>
      <parentid>494713</parentid>
      <timestamp>2025-11-21T00:35:51Z</timestamp>
      <contributor>
        <username>en&gt;Ferien</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:en:Module:Coordinates]]</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494715</id>
      <parentid>494714</parentid>
      <timestamp>2025-11-21T03:32:03Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Switch to template gadget category per request</comment>
      <origin>494534</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>494716</id>
      <parentid>494715</parentid>
      <timestamp>2025-11-21T05:34:27Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: Reverting edits from past 90 minutes</comment>
      <origin>494716</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="21639" sha1="hmaceynn8xe6j2ep8fvumc1x2f2n94m" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

local math_mod = require("Module:Math")
local coordinates = {};

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = '//tools.wmflabs.org/geohack/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(s, notes)
	local l = "[[Geographic coordinate system|Coordinates]]: " .. s
	local co = '&lt;span id="coordinates"&gt;' .. l .. notes .. '&lt;/span&gt;';
	return '&lt;span style="font-size: small;"&gt;' .. co .. '&lt;/span&gt;';
end

--[[ Helper function, Replacement for {{coord/display/inline}} ]]
local function displayinline(s, notes)
	return s .. notes	
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		local errorHTML = '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;'
		result = result .. errorHTML .. "&lt;br /&gt;"
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for 
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end
	
	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end
	
	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			 .. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			 .. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W" 
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end
	
	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			 .. geodeclat .. ' '
			 .. geodeclong
			 .. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			 .. coordinateSpec["dec-lat"] .. '; '
			 .. coordinateSpec["dec-long"]
			 .. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml 
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

	return '&lt;span class="plainlinks nourlexpansion"&gt;' .. 
		'[' .. coord_link .. uriComponents .. ' ' .. inner .. ']' .. '&lt;/span&gt;'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)	
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"
	
	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[ 
Helper function, convert decimal latitude or longitude to 
degrees, minutes, and seconds format based on the specified precision.  
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0
	
	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end
	
	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end
	
	local decimal = factor * (degrees+(minutes+seconds/60)/60) 
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[ 
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[ 
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.
		
		For not simply disable this check.
		
		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end	
		
	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end
	
	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the a
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}
	
	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end
	
	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );	
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}	
	
	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude 
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec = {}
	local errors = {}
	
	lat_f = lat_f:upper();
	long_f = long_f:upper();
	
	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		local t_d, t_m, t_s, t_f;
		t_d = lat_d;
		t_m = lat_m;
		t_s = lat_s;
		t_f = lat_f;
		lat_d = long_d;
		lat_m = long_m;
		lat_s = long_s;
		lat_f = long_f;
		long_d = t_d;
		long_m = t_m;
		long_s = t_s;
		long_f = t_f;
	end	
	
	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end
	
	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then 
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then 
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then 
				long_d = '-' .. long_d;
			end	 
			
			return parseDec( lat_d, long_d, format );
		end		
	end   
	
	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end   

	return coordinateSpec, errors
end

--[[ 
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local primary = false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end
	
	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end			  
		result.param = table.concat({args[1], 'N', args[2] or '', 'E', args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors = parseDMS(args[1], args[2], args[3], args[4], 
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors = parseDMS(args[1], args[2], nil, args[3], 
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors = parseDMS(args[1], nil, nil, args[2], 
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end	
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}})
	end
	result.name = args.name
	
	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then 
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end
	
	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories()
	local ret
	if mw.wikibase and current_page.namespace == 0 then
		local entity = mw.wikibase.getEntityObject()
		if entity and entity.claims and entity.claims.P625 and entity.claims.P625[1] then
			local snaktype = entity.claims.P625[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}
	
]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix | 
		negative_suffix | precision }}
	
decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end		

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees | 
		minutes | seconds }}
	
Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is 
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...
	
	Refer to {{coord}} documentation page for many additional parameters and 
	configuration options.
	
Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser 
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not args[1] and not args[2] and mw.wikibase.getEntityObject() then
		local entity = mw.wikibase.getEntityObject()
		if entity 
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1]=entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2]=entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision=-math_mod._round(math.log(precision)/math.log(10),0)
				args[1]=math_mod._round(args[1],precision)
				args[2]=math_mod._round(args[2],precision)
			end
		end
	end
	
	local contents = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end
	
	local text = ''
	if isInline(Display) then
		text = text .. displayinline(contents, Notes)
	end
	if isInTitle(Display) then
		text = text
			.. displaytitle(contents, Notes)
			.. makeWikidataCategories()
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates.coord2text(frame)
	if frame.args[1] == '' or frame.args[2] == '' or not frame.args[2] then return nil end
	frame.args[2] = mw.text.trim(frame.args[2])
	if frame.args[2] == 'lat' or frame.args[2] == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(frame.args[1],'%d+.?%d*°[NS] %d+.?%d*°[EW]') or ''), ' ')
		if frame.args[2] == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(frame.args[1], 'params=.-_'..frame.args[2]..':(.-)[ _]')
	end
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ', 1) end
		end
	end
	return frame.args[1]
end

return coordinates</text>
      <sha1>hmaceynn8xe6j2ep8fvumc1x2f2n94m</sha1>
    </revision>
    <revision>
      <id>494717</id>
      <parentid>494716</parentid>
      <timestamp>2025-11-21T05:49:59Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Switch to template gadget category per request</comment>
      <origin>494717</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25035" sha1="g0kvlzvbowr8hudeh9ex9bf1dawsj3q" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>g0kvlzvbowr8hudeh9ex9bf1dawsj3q</sha1>
    </revision>
    <revision>
      <id>476</id>
      <timestamp>2025-11-22T14:38:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Text replacement - "↵↵↵" to "
"</comment>
      <origin>476</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25033" sha1="qhrgqc7t9v24j2bunnd362586wfrbnr" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end
--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
		local result, negative = mw.text.split((mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]') or ''), ' ')
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
		result = mw.text.split(result, '°')
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2])
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>qhrgqc7t9v24j2bunnd362586wfrbnr</sha1>
    </revision>
    <revision>
      <id>3407370</id>
      <parentid>476</parentid>
      <timestamp>2026-01-25T20:35:45Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3407370</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25273" sha1="650yc7v4moy3ah89kzxr3k77zyofxp5" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.ff == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>650yc7v4moy3ah89kzxr3k77zyofxp5</sha1>
    </revision>
    <revision>
      <id>3482099</id>
      <parentid>3407370</parentid>
      <timestamp>2026-01-25T20:35:45Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3482099</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3405065</id>
      <parentid>476</parentid>
      <timestamp>2026-03-12T18:09:46Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3405066</id>
      <parentid>3405065</parentid>
      <timestamp>2026-03-12T18:09:47Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3407194</id>
      <parentid>3405066</parentid>
      <timestamp>2026-03-12T20:19:35Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3407195</id>
      <parentid>3407194</parentid>
      <timestamp>2026-03-12T20:19:36Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3407371</id>
      <parentid>3407195</parentid>
      <timestamp>2026-03-12T20:21:27Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3408479</id>
      <parentid>3407371</parentid>
      <timestamp>2026-03-12T20:38:16Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3408480</id>
      <parentid>3408479</parentid>
      <timestamp>2026-03-12T20:38:17Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439204</id>
      <parentid>3408480</parentid>
      <timestamp>2026-03-13T15:57:58Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439205</id>
      <parentid>3439204</parentid>
      <timestamp>2026-03-13T15:57:58Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439294</id>
      <parentid>3439205</parentid>
      <timestamp>2026-03-13T15:58:32Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439295</id>
      <parentid>3439294</parentid>
      <timestamp>2026-03-13T15:58:32Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439326</id>
      <parentid>3439295</parentid>
      <timestamp>2026-03-13T15:58:46Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439327</id>
      <parentid>3439326</parentid>
      <timestamp>2026-03-13T15:58:46Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439390</id>
      <parentid>3439327</parentid>
      <timestamp>2026-03-13T15:59:07Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439391</id>
      <parentid>3439390</parentid>
      <timestamp>2026-03-13T15:59:07Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439468</id>
      <parentid>3439391</parentid>
      <timestamp>2026-03-13T15:59:34Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439469</id>
      <parentid>3439468</parentid>
      <timestamp>2026-03-13T15:59:35Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439546</id>
      <parentid>3439469</parentid>
      <timestamp>2026-03-13T16:00:33Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439547</id>
      <parentid>3439546</parentid>
      <timestamp>2026-03-13T16:00:33Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439610</id>
      <parentid>3439547</parentid>
      <timestamp>2026-03-13T16:01:03Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439611</id>
      <parentid>3439610</parentid>
      <timestamp>2026-03-13T16:01:04Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439652</id>
      <parentid>3439611</parentid>
      <timestamp>2026-03-13T16:01:21Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439653</id>
      <parentid>3439652</parentid>
      <timestamp>2026-03-13T16:01:21Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439684</id>
      <parentid>3439653</parentid>
      <timestamp>2026-03-13T16:01:35Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439685</id>
      <parentid>3439684</parentid>
      <timestamp>2026-03-13T16:01:36Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439716</id>
      <parentid>3439685</parentid>
      <timestamp>2026-03-13T16:01:50Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3439717</id>
      <parentid>3439716</parentid>
      <timestamp>2026-03-13T16:01:50Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23052491609|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449499</id>
      <parentid>3439717</parentid>
      <timestamp>2026-03-13T19:46:48Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449500</id>
      <parentid>3449499</parentid>
      <timestamp>2026-03-13T19:46:48Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449589</id>
      <parentid>3449500</parentid>
      <timestamp>2026-03-13T19:47:20Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449590</id>
      <parentid>3449589</parentid>
      <timestamp>2026-03-13T19:47:21Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449621</id>
      <parentid>3449590</parentid>
      <timestamp>2026-03-13T19:47:33Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449622</id>
      <parentid>3449621</parentid>
      <timestamp>2026-03-13T19:47:33Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449685</id>
      <parentid>3449622</parentid>
      <timestamp>2026-03-13T19:47:50Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449686</id>
      <parentid>3449685</parentid>
      <timestamp>2026-03-13T19:47:51Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449763</id>
      <parentid>3449686</parentid>
      <timestamp>2026-03-13T19:48:21Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449764</id>
      <parentid>3449763</parentid>
      <timestamp>2026-03-13T19:48:21Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449841</id>
      <parentid>3449764</parentid>
      <timestamp>2026-03-13T19:49:21Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449842</id>
      <parentid>3449841</parentid>
      <timestamp>2026-03-13T19:49:21Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449919</id>
      <parentid>3449842</parentid>
      <timestamp>2026-03-13T19:50:51Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449920</id>
      <parentid>3449919</parentid>
      <timestamp>2026-03-13T19:50:51Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449997</id>
      <parentid>3449920</parentid>
      <timestamp>2026-03-13T19:52:50Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3449998</id>
      <parentid>3449997</parentid>
      <timestamp>2026-03-13T19:52:50Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450061</id>
      <parentid>3449998</parentid>
      <timestamp>2026-03-13T19:53:18Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450062</id>
      <parentid>3450061</parentid>
      <timestamp>2026-03-13T19:53:18Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450103</id>
      <parentid>3450062</parentid>
      <timestamp>2026-03-13T19:53:34Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450104</id>
      <parentid>3450103</parentid>
      <timestamp>2026-03-13T19:53:34Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450135</id>
      <parentid>3450104</parentid>
      <timestamp>2026-03-13T19:53:47Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450136</id>
      <parentid>3450135</parentid>
      <timestamp>2026-03-13T19:53:47Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450167</id>
      <parentid>3450136</parentid>
      <timestamp>2026-03-13T19:53:59Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450168</id>
      <parentid>3450167</parentid>
      <timestamp>2026-03-13T19:54:00Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450221</id>
      <parentid>3450168</parentid>
      <timestamp>2026-03-13T19:54:15Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3450222</id>
      <parentid>3450221</parentid>
      <timestamp>2026-03-13T19:54:15Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23053570201|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459839</id>
      <parentid>3450222</parentid>
      <timestamp>2026-03-14T03:55:25Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459840</id>
      <parentid>3459839</parentid>
      <timestamp>2026-03-14T03:55:26Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459873</id>
      <parentid>3459840</parentid>
      <timestamp>2026-03-14T03:55:42Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459874</id>
      <parentid>3459873</parentid>
      <timestamp>2026-03-14T03:55:42Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459909</id>
      <parentid>3459874</parentid>
      <timestamp>2026-03-14T03:55:57Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459910</id>
      <parentid>3459909</parentid>
      <timestamp>2026-03-14T03:55:57Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459941</id>
      <parentid>3459910</parentid>
      <timestamp>2026-03-14T03:56:11Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459942</id>
      <parentid>3459941</parentid>
      <timestamp>2026-03-14T03:56:12Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459973</id>
      <parentid>3459942</parentid>
      <timestamp>2026-03-14T03:56:25Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3459974</id>
      <parentid>3459973</parentid>
      <timestamp>2026-03-14T03:56:25Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460005</id>
      <parentid>3459974</parentid>
      <timestamp>2026-03-14T03:56:38Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460006</id>
      <parentid>3460005</parentid>
      <timestamp>2026-03-14T03:56:38Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460037</id>
      <parentid>3460006</parentid>
      <timestamp>2026-03-14T03:56:51Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460038</id>
      <parentid>3460037</parentid>
      <timestamp>2026-03-14T03:56:51Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460069</id>
      <parentid>3460038</parentid>
      <timestamp>2026-03-14T03:57:04Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460070</id>
      <parentid>3460069</parentid>
      <timestamp>2026-03-14T03:57:04Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460101</id>
      <parentid>3460070</parentid>
      <timestamp>2026-03-14T03:57:17Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460102</id>
      <parentid>3460101</parentid>
      <timestamp>2026-03-14T03:57:18Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460133</id>
      <parentid>3460102</parentid>
      <timestamp>2026-03-14T03:57:30Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3460134</id>
      <parentid>3460133</parentid>
      <timestamp>2026-03-14T03:57:31Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23076956737|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461044</id>
      <parentid>3460134</parentid>
      <timestamp>2026-03-14T05:41:22Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461045</id>
      <parentid>3461044</parentid>
      <timestamp>2026-03-14T05:41:22Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081580192|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461076</id>
      <parentid>3461045</parentid>
      <timestamp>2026-03-14T05:41:35Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461077</id>
      <parentid>3461076</parentid>
      <timestamp>2026-03-14T05:41:35Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081580192|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461110</id>
      <parentid>3461077</parentid>
      <timestamp>2026-03-14T05:41:49Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461111</id>
      <parentid>3461110</parentid>
      <timestamp>2026-03-14T05:41:50Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081580192|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461150</id>
      <parentid>3461111</parentid>
      <timestamp>2026-03-14T05:42:03Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461151</id>
      <parentid>3461150</parentid>
      <timestamp>2026-03-14T05:42:04Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081580192|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461182</id>
      <parentid>3461151</parentid>
      <timestamp>2026-03-14T05:42:16Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461183</id>
      <parentid>3461182</parentid>
      <timestamp>2026-03-14T05:42:17Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081580192|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461214</id>
      <parentid>3461183</parentid>
      <timestamp>2026-03-14T05:42:30Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461215</id>
      <parentid>3461214</parentid>
      <timestamp>2026-03-14T05:42:30Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081580192|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461246</id>
      <parentid>3461215</parentid>
      <timestamp>2026-03-14T05:42:43Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461247</id>
      <parentid>3461246</parentid>
      <timestamp>2026-03-14T05:42:43Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081580192|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461278</id>
      <parentid>3461247</parentid>
      <timestamp>2026-03-14T05:42:57Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3461279</id>
      <parentid>3461278</parentid>
      <timestamp>2026-03-14T05:42:57Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081580192|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462117</id>
      <parentid>3461279</parentid>
      <timestamp>2026-03-14T06:30:04Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462118</id>
      <parentid>3462117</parentid>
      <timestamp>2026-03-14T06:30:04Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081942775|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462149</id>
      <parentid>3462118</parentid>
      <timestamp>2026-03-14T06:30:16Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462150</id>
      <parentid>3462149</parentid>
      <timestamp>2026-03-14T06:30:16Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081942775|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462183</id>
      <parentid>3462150</parentid>
      <timestamp>2026-03-14T06:30:29Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462184</id>
      <parentid>3462183</parentid>
      <timestamp>2026-03-14T06:30:29Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081942775|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462223</id>
      <parentid>3462184</parentid>
      <timestamp>2026-03-14T06:30:43Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462224</id>
      <parentid>3462223</parentid>
      <timestamp>2026-03-14T06:30:43Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081942775|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462255</id>
      <parentid>3462224</parentid>
      <timestamp>2026-03-14T06:30:55Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462256</id>
      <parentid>3462255</parentid>
      <timestamp>2026-03-14T06:30:56Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081942775|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462287</id>
      <parentid>3462256</parentid>
      <timestamp>2026-03-14T06:31:07Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462288</id>
      <parentid>3462287</parentid>
      <timestamp>2026-03-14T06:31:07Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081942775|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462319</id>
      <parentid>3462288</parentid>
      <timestamp>2026-03-14T06:31:19Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462320</id>
      <parentid>3462319</parentid>
      <timestamp>2026-03-14T06:31:19Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081942775|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462351</id>
      <parentid>3462320</parentid>
      <timestamp>2026-03-14T06:31:31Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3462352</id>
      <parentid>3462351</parentid>
      <timestamp>2026-03-14T06:31:31Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23081942775|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471522</id>
      <parentid>3462352</parentid>
      <timestamp>2026-03-15T04:08:05Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471523</id>
      <parentid>3471522</parentid>
      <timestamp>2026-03-15T04:08:06Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23100572874|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471554</id>
      <parentid>3471523</parentid>
      <timestamp>2026-03-15T04:08:20Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471555</id>
      <parentid>3471554</parentid>
      <timestamp>2026-03-15T04:08:20Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23100572874|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471588</id>
      <parentid>3471555</parentid>
      <timestamp>2026-03-15T04:08:35Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471589</id>
      <parentid>3471588</parentid>
      <timestamp>2026-03-15T04:08:35Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23100572874|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471628</id>
      <parentid>3471589</parentid>
      <timestamp>2026-03-15T04:08:50Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471629</id>
      <parentid>3471628</parentid>
      <timestamp>2026-03-15T04:08:50Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23100572874|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471660</id>
      <parentid>3471629</parentid>
      <timestamp>2026-03-15T04:09:03Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471661</id>
      <parentid>3471660</parentid>
      <timestamp>2026-03-15T04:09:03Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23100572874|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471692</id>
      <parentid>3471661</parentid>
      <timestamp>2026-03-15T04:09:16Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471693</id>
      <parentid>3471692</parentid>
      <timestamp>2026-03-15T04:09:16Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23100572874|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471724</id>
      <parentid>3471693</parentid>
      <timestamp>2026-03-15T04:09:29Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471725</id>
      <parentid>3471724</parentid>
      <timestamp>2026-03-15T04:09:30Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23100572874|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471756</id>
      <parentid>3471725</parentid>
      <timestamp>2026-03-15T04:09:43Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3471757</id>
      <parentid>3471756</parentid>
      <timestamp>2026-03-15T04:09:44Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23100572874|time triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3478991</id>
      <parentid>3471757</parentid>
      <timestamp>2026-03-16T00:39:52Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3478992</id>
      <parentid>3478991</parentid>
      <timestamp>2026-03-16T00:39:52Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479023</id>
      <parentid>3478992</parentid>
      <timestamp>2026-03-16T00:40:06Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479024</id>
      <parentid>3479023</parentid>
      <timestamp>2026-03-16T00:40:07Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479057</id>
      <parentid>3479024</parentid>
      <timestamp>2026-03-16T00:40:21Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479058</id>
      <parentid>3479057</parentid>
      <timestamp>2026-03-16T00:40:21Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479097</id>
      <parentid>3479058</parentid>
      <timestamp>2026-03-16T00:40:36Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479098</id>
      <parentid>3479097</parentid>
      <timestamp>2026-03-16T00:40:37Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479129</id>
      <parentid>3479098</parentid>
      <timestamp>2026-03-16T00:40:57Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479130</id>
      <parentid>3479129</parentid>
      <timestamp>2026-03-16T00:40:57Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479161</id>
      <parentid>3479130</parentid>
      <timestamp>2026-03-16T00:41:16Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479162</id>
      <parentid>3479161</parentid>
      <timestamp>2026-03-16T00:41:16Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479193</id>
      <parentid>3479162</parentid>
      <timestamp>2026-03-16T00:41:31Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479194</id>
      <parentid>3479193</parentid>
      <timestamp>2026-03-16T00:41:31Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479225</id>
      <parentid>3479194</parentid>
      <timestamp>2026-03-16T00:41:45Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479226</id>
      <parentid>3479225</parentid>
      <timestamp>2026-03-16T00:41:45Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479257</id>
      <parentid>3479226</parentid>
      <timestamp>2026-03-16T00:42:00Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479258</id>
      <parentid>3479257</parentid>
      <timestamp>2026-03-16T00:42:00Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123126521|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479478</id>
      <parentid>3479258</parentid>
      <timestamp>2026-03-16T01:13:01Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479479</id>
      <parentid>3479478</parentid>
      <timestamp>2026-03-16T01:13:01Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479510</id>
      <parentid>3479479</parentid>
      <timestamp>2026-03-16T01:13:14Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479511</id>
      <parentid>3479510</parentid>
      <timestamp>2026-03-16T01:13:14Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479544</id>
      <parentid>3479511</parentid>
      <timestamp>2026-03-16T01:13:27Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479545</id>
      <parentid>3479544</parentid>
      <timestamp>2026-03-16T01:13:27Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479584</id>
      <parentid>3479545</parentid>
      <timestamp>2026-03-16T01:13:41Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479585</id>
      <parentid>3479584</parentid>
      <timestamp>2026-03-16T01:13:41Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479616</id>
      <parentid>3479585</parentid>
      <timestamp>2026-03-16T01:13:54Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479617</id>
      <parentid>3479616</parentid>
      <timestamp>2026-03-16T01:13:54Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479648</id>
      <parentid>3479617</parentid>
      <timestamp>2026-03-16T01:14:06Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479649</id>
      <parentid>3479648</parentid>
      <timestamp>2026-03-16T01:14:07Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479680</id>
      <parentid>3479649</parentid>
      <timestamp>2026-03-16T01:14:19Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479681</id>
      <parentid>3479680</parentid>
      <timestamp>2026-03-16T01:14:19Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479712</id>
      <parentid>3479681</parentid>
      <timestamp>2026-03-16T01:14:32Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479713</id>
      <parentid>3479712</parentid>
      <timestamp>2026-03-16T01:14:32Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479744</id>
      <parentid>3479713</parentid>
      <timestamp>2026-03-16T01:14:44Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3479745</id>
      <parentid>3479744</parentid>
      <timestamp>2026-03-16T01:14:44Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23123802722|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480059</id>
      <parentid>3479745</parentid>
      <timestamp>2026-03-16T01:45:33Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480060</id>
      <parentid>3480059</parentid>
      <timestamp>2026-03-16T01:45:33Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480091</id>
      <parentid>3480060</parentid>
      <timestamp>2026-03-16T01:45:46Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480092</id>
      <parentid>3480091</parentid>
      <timestamp>2026-03-16T01:45:46Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480125</id>
      <parentid>3480092</parentid>
      <timestamp>2026-03-16T01:45:59Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480126</id>
      <parentid>3480125</parentid>
      <timestamp>2026-03-16T01:45:59Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480165</id>
      <parentid>3480126</parentid>
      <timestamp>2026-03-16T01:46:13Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480166</id>
      <parentid>3480165</parentid>
      <timestamp>2026-03-16T01:46:14Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480197</id>
      <parentid>3480166</parentid>
      <timestamp>2026-03-16T01:46:26Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480198</id>
      <parentid>3480197</parentid>
      <timestamp>2026-03-16T01:46:26Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480229</id>
      <parentid>3480198</parentid>
      <timestamp>2026-03-16T01:46:40Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480230</id>
      <parentid>3480229</parentid>
      <timestamp>2026-03-16T01:46:40Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480261</id>
      <parentid>3480230</parentid>
      <timestamp>2026-03-16T01:46:53Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480262</id>
      <parentid>3480261</parentid>
      <timestamp>2026-03-16T01:46:53Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480293</id>
      <parentid>3480262</parentid>
      <timestamp>2026-03-16T01:47:06Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480294</id>
      <parentid>3480293</parentid>
      <timestamp>2026-03-16T01:47:06Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480325</id>
      <parentid>3480294</parentid>
      <timestamp>2026-03-16T01:47:19Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480326</id>
      <parentid>3480325</parentid>
      <timestamp>2026-03-16T01:47:20Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23124421395|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480745</id>
      <parentid>3480326</parentid>
      <timestamp>2026-03-16T02:35:24Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480746</id>
      <parentid>3480745</parentid>
      <timestamp>2026-03-16T02:35:24Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480777</id>
      <parentid>3480746</parentid>
      <timestamp>2026-03-16T02:35:37Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480778</id>
      <parentid>3480777</parentid>
      <timestamp>2026-03-16T02:35:37Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480811</id>
      <parentid>3480778</parentid>
      <timestamp>2026-03-16T02:35:51Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480812</id>
      <parentid>3480811</parentid>
      <timestamp>2026-03-16T02:35:51Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480851</id>
      <parentid>3480812</parentid>
      <timestamp>2026-03-16T02:36:04Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480852</id>
      <parentid>3480851</parentid>
      <timestamp>2026-03-16T02:36:05Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480883</id>
      <parentid>3480852</parentid>
      <timestamp>2026-03-16T02:36:17Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480884</id>
      <parentid>3480883</parentid>
      <timestamp>2026-03-16T02:36:18Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480915</id>
      <parentid>3480884</parentid>
      <timestamp>2026-03-16T02:36:30Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480916</id>
      <parentid>3480915</parentid>
      <timestamp>2026-03-16T02:36:31Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480947</id>
      <parentid>3480916</parentid>
      <timestamp>2026-03-16T02:36:44Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480948</id>
      <parentid>3480947</parentid>
      <timestamp>2026-03-16T02:36:44Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480979</id>
      <parentid>3480948</parentid>
      <timestamp>2026-03-16T02:36:57Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3480980</id>
      <parentid>3480979</parentid>
      <timestamp>2026-03-16T02:36:57Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481011</id>
      <parentid>3480980</parentid>
      <timestamp>2026-03-16T02:37:10Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481012</id>
      <parentid>3481011</parentid>
      <timestamp>2026-03-16T02:37:11Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23125537382|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481428</id>
      <parentid>3481012</parentid>
      <timestamp>2026-03-16T04:12:04Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481429</id>
      <parentid>3481428</parentid>
      <timestamp>2026-03-16T04:12:04Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481460</id>
      <parentid>3481429</parentid>
      <timestamp>2026-03-16T04:12:17Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481461</id>
      <parentid>3481460</parentid>
      <timestamp>2026-03-16T04:12:18Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481494</id>
      <parentid>3481461</parentid>
      <timestamp>2026-03-16T04:12:31Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481495</id>
      <parentid>3481494</parentid>
      <timestamp>2026-03-16T04:12:31Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481534</id>
      <parentid>3481495</parentid>
      <timestamp>2026-03-16T04:12:45Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481535</id>
      <parentid>3481534</parentid>
      <timestamp>2026-03-16T04:12:45Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481566</id>
      <parentid>3481535</parentid>
      <timestamp>2026-03-16T04:12:58Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481567</id>
      <parentid>3481566</parentid>
      <timestamp>2026-03-16T04:12:58Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481598</id>
      <parentid>3481567</parentid>
      <timestamp>2026-03-16T04:13:11Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481599</id>
      <parentid>3481598</parentid>
      <timestamp>2026-03-16T04:13:11Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481630</id>
      <parentid>3481599</parentid>
      <timestamp>2026-03-16T04:13:24Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481631</id>
      <parentid>3481630</parentid>
      <timestamp>2026-03-16T04:13:24Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481662</id>
      <parentid>3481631</parentid>
      <timestamp>2026-03-16T04:13:36Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481663</id>
      <parentid>3481662</parentid>
      <timestamp>2026-03-16T04:13:36Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481694</id>
      <parentid>3481663</parentid>
      <timestamp>2026-03-16T04:13:49Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3481695</id>
      <parentid>3481694</parentid>
      <timestamp>2026-03-16T04:13:49Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23127601988|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3482100</id>
      <parentid>3481695</parentid>
      <timestamp>2026-03-16T05:06:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487467</id>
      <parentid>3482100</parentid>
      <timestamp>2026-03-16T07:53:22Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487468</id>
      <parentid>3487467</parentid>
      <timestamp>2026-03-16T07:53:22Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487499</id>
      <parentid>3487468</parentid>
      <timestamp>2026-03-16T07:53:35Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487500</id>
      <parentid>3487499</parentid>
      <timestamp>2026-03-16T07:53:36Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487533</id>
      <parentid>3487500</parentid>
      <timestamp>2026-03-16T07:53:50Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487534</id>
      <parentid>3487533</parentid>
      <timestamp>2026-03-16T07:53:51Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487573</id>
      <parentid>3487534</parentid>
      <timestamp>2026-03-16T07:54:05Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487574</id>
      <parentid>3487573</parentid>
      <timestamp>2026-03-16T07:54:05Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487605</id>
      <parentid>3487574</parentid>
      <timestamp>2026-03-16T07:54:18Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487606</id>
      <parentid>3487605</parentid>
      <timestamp>2026-03-16T07:54:18Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487637</id>
      <parentid>3487606</parentid>
      <timestamp>2026-03-16T07:54:31Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487638</id>
      <parentid>3487637</parentid>
      <timestamp>2026-03-16T07:54:31Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487669</id>
      <parentid>3487638</parentid>
      <timestamp>2026-03-16T07:54:44Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487670</id>
      <parentid>3487669</parentid>
      <timestamp>2026-03-16T07:54:44Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487701</id>
      <parentid>3487670</parentid>
      <timestamp>2026-03-16T07:54:57Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487702</id>
      <parentid>3487701</parentid>
      <timestamp>2026-03-16T07:54:58Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487733</id>
      <parentid>3487702</parentid>
      <timestamp>2026-03-16T07:55:12Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3487734</id>
      <parentid>3487733</parentid>
      <timestamp>2026-03-16T07:55:12Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23128882600|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493564</id>
      <parentid>3487734</parentid>
      <timestamp>2026-03-16T18:45:26Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493565</id>
      <parentid>3493564</parentid>
      <timestamp>2026-03-16T18:45:26Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493596</id>
      <parentid>3493565</parentid>
      <timestamp>2026-03-16T18:45:39Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493597</id>
      <parentid>3493596</parentid>
      <timestamp>2026-03-16T18:45:39Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493630</id>
      <parentid>3493597</parentid>
      <timestamp>2026-03-16T18:45:54Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493631</id>
      <parentid>3493630</parentid>
      <timestamp>2026-03-16T18:45:55Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493670</id>
      <parentid>3493631</parentid>
      <timestamp>2026-03-16T18:46:09Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493671</id>
      <parentid>3493670</parentid>
      <timestamp>2026-03-16T18:46:10Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493702</id>
      <parentid>3493671</parentid>
      <timestamp>2026-03-16T18:46:23Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493703</id>
      <parentid>3493702</parentid>
      <timestamp>2026-03-16T18:46:24Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493734</id>
      <parentid>3493703</parentid>
      <timestamp>2026-03-16T18:46:38Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493735</id>
      <parentid>3493734</parentid>
      <timestamp>2026-03-16T18:46:38Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493766</id>
      <parentid>3493735</parentid>
      <timestamp>2026-03-16T18:46:53Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493767</id>
      <parentid>3493766</parentid>
      <timestamp>2026-03-16T18:46:53Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493798</id>
      <parentid>3493767</parentid>
      <timestamp>2026-03-16T18:47:07Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493799</id>
      <parentid>3493798</parentid>
      <timestamp>2026-03-16T18:47:07Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493830</id>
      <parentid>3493799</parentid>
      <timestamp>2026-03-16T18:47:21Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3493831</id>
      <parentid>3493830</parentid>
      <timestamp>2026-03-16T18:47:21Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23153182905|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499665</id>
      <parentid>3493831</parentid>
      <timestamp>2026-03-17T00:53:01Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499666</id>
      <parentid>3499665</parentid>
      <timestamp>2026-03-17T00:53:01Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499697</id>
      <parentid>3499666</parentid>
      <timestamp>2026-03-17T00:53:16Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499698</id>
      <parentid>3499697</parentid>
      <timestamp>2026-03-17T00:53:16Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499731</id>
      <parentid>3499698</parentid>
      <timestamp>2026-03-17T00:53:32Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499732</id>
      <parentid>3499731</parentid>
      <timestamp>2026-03-17T00:53:32Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499771</id>
      <parentid>3499732</parentid>
      <timestamp>2026-03-17T00:53:48Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499772</id>
      <parentid>3499771</parentid>
      <timestamp>2026-03-17T00:53:48Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499803</id>
      <parentid>3499772</parentid>
      <timestamp>2026-03-17T00:54:06Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499804</id>
      <parentid>3499803</parentid>
      <timestamp>2026-03-17T00:54:06Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499835</id>
      <parentid>3499804</parentid>
      <timestamp>2026-03-17T00:54:21Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499836</id>
      <parentid>3499835</parentid>
      <timestamp>2026-03-17T00:54:21Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499867</id>
      <parentid>3499836</parentid>
      <timestamp>2026-03-17T00:54:36Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499868</id>
      <parentid>3499867</parentid>
      <timestamp>2026-03-17T00:54:37Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499899</id>
      <parentid>3499868</parentid>
      <timestamp>2026-03-17T00:54:51Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499900</id>
      <parentid>3499899</parentid>
      <timestamp>2026-03-17T00:54:51Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499931</id>
      <parentid>3499900</parentid>
      <timestamp>2026-03-17T00:55:05Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3499932</id>
      <parentid>3499931</parentid>
      <timestamp>2026-03-17T00:55:05Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23168229401|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3509955</id>
      <parentid>3499932</parentid>
      <timestamp>2026-03-17T06:04:32Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3509956</id>
      <parentid>3509955</parentid>
      <timestamp>2026-03-17T06:04:32Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3509987</id>
      <parentid>3509956</parentid>
      <timestamp>2026-03-17T06:04:45Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3509988</id>
      <parentid>3509987</parentid>
      <timestamp>2026-03-17T06:04:45Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510021</id>
      <parentid>3509988</parentid>
      <timestamp>2026-03-17T06:04:59Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510022</id>
      <parentid>3510021</parentid>
      <timestamp>2026-03-17T06:05:00Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510061</id>
      <parentid>3510022</parentid>
      <timestamp>2026-03-17T06:05:14Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510062</id>
      <parentid>3510061</parentid>
      <timestamp>2026-03-17T06:05:15Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510093</id>
      <parentid>3510062</parentid>
      <timestamp>2026-03-17T06:05:29Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510094</id>
      <parentid>3510093</parentid>
      <timestamp>2026-03-17T06:05:29Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510125</id>
      <parentid>3510094</parentid>
      <timestamp>2026-03-17T06:05:42Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510126</id>
      <parentid>3510125</parentid>
      <timestamp>2026-03-17T06:05:43Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510157</id>
      <parentid>3510126</parentid>
      <timestamp>2026-03-17T06:05:56Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510158</id>
      <parentid>3510157</parentid>
      <timestamp>2026-03-17T06:05:56Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510189</id>
      <parentid>3510158</parentid>
      <timestamp>2026-03-17T06:06:10Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510190</id>
      <parentid>3510189</parentid>
      <timestamp>2026-03-17T06:06:10Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510221</id>
      <parentid>3510190</parentid>
      <timestamp>2026-03-17T06:06:23Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3510222</id>
      <parentid>3510221</parentid>
      <timestamp>2026-03-17T06:06:23Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23176857634|manual triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516027</id>
      <parentid>3510222</parentid>
      <timestamp>2026-03-17T09:29:03Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516028</id>
      <parentid>3516027</parentid>
      <timestamp>2026-03-17T09:29:03Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516059</id>
      <parentid>3516028</parentid>
      <timestamp>2026-03-17T09:29:16Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516060</id>
      <parentid>3516059</parentid>
      <timestamp>2026-03-17T09:29:16Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516093</id>
      <parentid>3516060</parentid>
      <timestamp>2026-03-17T09:29:29Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516094</id>
      <parentid>3516093</parentid>
      <timestamp>2026-03-17T09:29:30Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516133</id>
      <parentid>3516094</parentid>
      <timestamp>2026-03-17T09:29:43Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516134</id>
      <parentid>3516133</parentid>
      <timestamp>2026-03-17T09:29:44Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516165</id>
      <parentid>3516134</parentid>
      <timestamp>2026-03-17T09:29:56Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516166</id>
      <parentid>3516165</parentid>
      <timestamp>2026-03-17T09:29:56Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516197</id>
      <parentid>3516166</parentid>
      <timestamp>2026-03-17T09:30:09Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516198</id>
      <parentid>3516197</parentid>
      <timestamp>2026-03-17T09:30:09Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516229</id>
      <parentid>3516198</parentid>
      <timestamp>2026-03-17T09:30:21Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516230</id>
      <parentid>3516229</parentid>
      <timestamp>2026-03-17T09:30:22Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516261</id>
      <parentid>3516230</parentid>
      <timestamp>2026-03-17T09:30:34Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516262</id>
      <parentid>3516261</parentid>
      <timestamp>2026-03-17T09:30:35Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516293</id>
      <parentid>3516262</parentid>
      <timestamp>2026-03-17T09:30:47Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516294</id>
      <parentid>3516293</parentid>
      <timestamp>2026-03-17T09:30:48Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23177433986|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516713</id>
      <parentid>3516294</parentid>
      <timestamp>2026-03-17T17:42:52Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516714</id>
      <parentid>3516713</parentid>
      <timestamp>2026-03-17T17:42:52Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23208182735|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516745</id>
      <parentid>3516714</parentid>
      <timestamp>2026-03-17T17:43:07Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3516746</id>
      <parentid>3516745</parentid>
      <timestamp>2026-03-17T17:43:07Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23208182735|commit triggered pipeline]]</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554069</id>
      <parentid>3516746</parentid>
      <timestamp>2026-03-20T18:20:04Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554070</id>
      <parentid>3554069</parentid>
      <timestamp>2026-03-20T18:20:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554283</id>
      <parentid>3554070</parentid>
      <timestamp>2026-03-20T18:21:04Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554284</id>
      <parentid>3554283</parentid>
      <timestamp>2026-03-20T18:21:04Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554497</id>
      <parentid>3554284</parentid>
      <timestamp>2026-03-20T18:22:31Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554498</id>
      <parentid>3554497</parentid>
      <timestamp>2026-03-20T18:22:32Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554711</id>
      <parentid>3554498</parentid>
      <timestamp>2026-03-20T18:24:32Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554712</id>
      <parentid>3554711</parentid>
      <timestamp>2026-03-20T18:24:32Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554925</id>
      <parentid>3554712</parentid>
      <timestamp>2026-03-20T18:27:02Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3554926</id>
      <parentid>3554925</parentid>
      <timestamp>2026-03-20T18:27:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555139</id>
      <parentid>3554926</parentid>
      <timestamp>2026-03-20T18:30:05Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555140</id>
      <parentid>3555139</parentid>
      <timestamp>2026-03-20T18:30:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555353</id>
      <parentid>3555140</parentid>
      <timestamp>2026-03-20T18:33:35Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555354</id>
      <parentid>3555353</parentid>
      <timestamp>2026-03-20T18:33:35Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555567</id>
      <parentid>3555354</parentid>
      <timestamp>2026-03-20T18:37:40Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555568</id>
      <parentid>3555567</parentid>
      <timestamp>2026-03-20T18:37:40Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555781</id>
      <parentid>3555568</parentid>
      <timestamp>2026-03-20T18:42:09Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555782</id>
      <parentid>3555781</parentid>
      <timestamp>2026-03-20T18:42:09Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555995</id>
      <parentid>3555782</parentid>
      <timestamp>2026-03-20T18:47:12Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3555996</id>
      <parentid>3555995</parentid>
      <timestamp>2026-03-20T18:47:12Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3556209</id>
      <parentid>3555996</parentid>
      <timestamp>2026-03-20T18:52:41Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3556210</id>
      <parentid>3556209</parentid>
      <timestamp>2026-03-20T18:52:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3556423</id>
      <parentid>3556210</parentid>
      <timestamp>2026-03-20T18:58:46Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3556424</id>
      <parentid>3556423</parentid>
      <timestamp>2026-03-20T18:58:47Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3556637</id>
      <parentid>3556424</parentid>
      <timestamp>2026-03-20T19:05:16Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3556638</id>
      <parentid>3556637</parentid>
      <timestamp>2026-03-20T19:05:16Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3556851</id>
      <parentid>3556638</parentid>
      <timestamp>2026-03-20T19:12:18Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3556852</id>
      <parentid>3556851</parentid>
      <timestamp>2026-03-20T19:12:18Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3557115</id>
      <parentid>3556852</parentid>
      <timestamp>2026-03-20T19:37:59Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3557116</id>
      <parentid>3557115</parentid>
      <timestamp>2026-03-20T19:37:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3557437</id>
      <parentid>3557116</parentid>
      <timestamp>2026-03-20T19:41:57Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3557438</id>
      <parentid>3557437</parentid>
      <timestamp>2026-03-20T19:41:57Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3557952</id>
      <parentid>3557438</parentid>
      <timestamp>2026-03-20T19:46:29Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3557953</id>
      <parentid>3557952</parentid>
      <timestamp>2026-03-20T19:46:30Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3558819</id>
      <parentid>3557953</parentid>
      <timestamp>2026-03-20T19:55:31Z</timestamp>
      <contributor>
        <username>en&gt;Pppery</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3558820</id>
      <parentid>3558819</parentid>
      <timestamp>2026-03-20T19:55:32Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3560983</id>
      <parentid>3558820</parentid>
      <timestamp>2026-03-21T20:11:08Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
    <revision>
      <id>3560984</id>
      <parentid>3560983</parentid>
      <timestamp>2026-03-21T20:11:08Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3405065</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="25280" sha1="4j0gstvbrnunfsl9d87kcwlvt5s11ed" xml:space="preserve">--[[
This module is intended to replace the functionality of {{Coord}} and related
templates.  It provides several methods, including

{{#invoke:Coordinates | coord }} : General function formatting and displaying
coordinate values.

{{#invoke:Coordinates | dec2dms }} : Simple function for converting decimal
degree values to DMS format.

{{#invoke:Coordinates | dms2dec }} : Simple function for converting DMS format
to decimal degree format.

{{#invoke:Coordinates | link }} : Export the link used to reach the tools

]]

require('strict')

local math_mod = require("Module:Math")
local coordinates = {};
local isSandbox = mw.getCurrentFrame():getTitle():find('sandbox', 1, true);

local current_page = mw.title.getCurrentTitle()
local page_name = mw.uri.encode( current_page.prefixedText, 'WIKI' );
local coord_link = 'https://geohack.toolforge.org/geohack.php?pagename=' .. page_name .. '&amp;params='

--[[ Helper function, replacement for {{coord/display/title}} ]]
local function displaytitle(coords)
	return mw.getCurrentFrame():extensionTag{
		name = 'indicator',
		args = { name = 'coordinates' },
		content = '&lt;span id="coordinates"&gt;[[Geographic coordinate system|Coordinates]]: ' .. coords .. '&lt;/span&gt;'
	}
end

--[[ Helper function, used in detecting DMS formatting ]]
local function dmsTest(first, second)
	if type(first) ~= 'string' or type(second) ~= 'string' then
		return nil
	end
	local s = (first .. second):upper()
	return s:find('^[NS][EW]$') or s:find('^[EW][NS]$')
end


--[[ Wrapper function to grab args, see Module:Arguments for this function's documentation. ]]
local function makeInvokeFunc(funcName)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = 'Template:Coord'
		})
		return coordinates[funcName](args, frame)
	end
end

--[[ Helper function, handle optional args. ]]
local function optionalArg(arg, supplement)
	return arg and arg .. supplement or ''
end

--[[
Formats any error messages generated for display
]]
local function errorPrinter(errors)
	local result = ""
	for i,v in ipairs(errors) do
		result = result .. '&lt;strong class="error"&gt;Coordinates: ' .. v[2] .. '&lt;/strong&gt;&lt;br /&gt;'
	end
	return result
end

--[[
Determine the required CSS class to display coordinates

Usually geo-nondefault is hidden by CSS, unless a user has overridden this for himself
default is the mode as specificied by the user when calling the {{coord}} template
mode is the display mode (dec or dms) that we will need to determine the css class for
]]
local function displayDefault(default, mode)
	if default == "" then
		default = "dec"
	end

	if default == mode then
		return "geo-default"
	else
		return "geo-nondefault"
	end
end

--[[
specPrinter

Output formatter.  Takes the structure generated by either parseDec
or parseDMS and formats it for inclusion on Wikipedia.
]]
local function specPrinter(args, coordinateSpec)
	local uriComponents = coordinateSpec["param"]
	if uriComponents == "" then
		-- RETURN error, should never be empty or nil
		return "ERROR param was empty"
	end
	if args["name"] then
		uriComponents = uriComponents .. "&amp;title=" .. mw.uri.encode(coordinateSpec["name"])
	end

	local geodmshtml = '&lt;span class="geo-dms" title="Maps, aerial photos, and other data for this location"&gt;'
			.. '&lt;span class="latitude"&gt;' .. coordinateSpec["dms-lat"] .. '&lt;/span&gt; '
			.. '&lt;span class="longitude"&gt;' ..coordinateSpec["dms-long"] .. '&lt;/span&gt;'
			.. '&lt;/span&gt;'

	local lat = tonumber( coordinateSpec["dec-lat"] ) or 0
	local geodeclat
	if lat &lt; 0 then
		-- FIXME this breaks the pre-existing precision
		geodeclat = tostring(coordinateSpec["dec-lat"]):sub(2) .. "°S"
	else
		geodeclat = (coordinateSpec["dec-lat"] or 0) .. "°N"
	end

	local long = tonumber( coordinateSpec["dec-long"] ) or 0
	local geodeclong
	if long &lt; 0 then
		-- FIXME does not handle unicode minus
		geodeclong = tostring(coordinateSpec["dec-long"]):sub(2) .. "°W"
	else
		geodeclong = (coordinateSpec["dec-long"] or 0) .. "°E"
	end

	local geodechtml = '&lt;span class="geo-dec" title="Maps, aerial photos, and other data for this location"&gt;'
			.. geodeclat .. ' '
			.. geodeclong
			.. '&lt;/span&gt;'

	local geonumhtml = '&lt;span class="geo"&gt;'
			.. coordinateSpec["dec-lat"] .. '; '
			.. coordinateSpec["dec-long"]
			.. '&lt;/span&gt;'

	local inner = '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dms" ) .. '"&gt;' .. geodmshtml .. '&lt;/span&gt;'
				.. '&lt;span class="geo-multi-punct"&gt;&amp;#xfeff; / &amp;#xfeff;&lt;/span&gt;'
				.. '&lt;span class="' .. displayDefault(coordinateSpec["default"], "dec" ) .. '"&gt;';

	if not args["name"] then
		inner = inner .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;&lt;/span&gt;'
	else
		inner = inner .. '&lt;span class="vcard"&gt;' .. geodechtml
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; / ' .. geonumhtml .. '&lt;/span&gt;'
				.. '&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;'
				.. args["name"] .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;'
	end

    local stylesheetLink = 'Module:Coordinates' .. ( isSandbox and '/sandbox' or '' ) .. '/styles.css'
	return mw.getCurrentFrame():extensionTag{
		name = 'templatestyles', args = { src = stylesheetLink }
	} .. '&lt;span class="plainlinks nourlexpansion"&gt;[' .. coord_link .. uriComponents ..
	' ' .. inner .. ']&lt;/span&gt;' .. '[[Category:Pages using gadget WikiMiniAtlas]]'
end

--[[ Helper function, convert decimal to degrees ]]
local function convert_dec2dms_d(coordinate)
	local d = math_mod._round( coordinate, 0 ) .. "°"
	return d .. ""
end

--[[ Helper function, convert decimal to degrees and minutes ]]
local function convert_dec2dms_dm(coordinate)
	coordinate = math_mod._round( coordinate * 60, 0 );
	local m = coordinate % 60;
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m )
end

--[[ Helper function, convert decimal to degrees, minutes, and seconds ]]
local function convert_dec2dms_dms(coordinate)
	coordinate = math_mod._round( coordinate * 60 * 60, 0 );
	local s = coordinate % 60
	coordinate = math.floor( (coordinate - s) / 60 );
	local m = coordinate % 60
	coordinate = math.floor( (coordinate - m) / 60 );
	local d = coordinate % 360 .."°"

	return d .. string.format( "%02d′", m ) .. string.format( "%02d″", s )
end

--[[
Helper function, convert decimal latitude or longitude to
degrees, minutes, and seconds format based on the specified precision.
]]
local function convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
	local coord = tonumber(coordinate)
	local postfix
	if coord &gt;= 0 then
		postfix = firstPostfix
	else
		postfix = secondPostfix
	end

	precision = precision:lower();
	if precision == "dms" then
		return convert_dec2dms_dms( math.abs( coord ) ) .. postfix;
	elseif precision == "dm" then
		return convert_dec2dms_dm( math.abs( coord ) ) .. postfix;
	elseif precision == "d" then
		return convert_dec2dms_d( math.abs( coord ) ) .. postfix;
	end
end

--[[
Convert DMS format into a N or E decimal coordinate
]]
local function convert_dms2dec(direction, degrees_str, minutes_str, seconds_str)
	local degrees = tonumber(degrees_str)
	local minutes = tonumber(minutes_str) or 0
	local seconds = tonumber(seconds_str) or 0

	local factor = 1
	if direction == "S" or direction == "W" then
		factor = -1
	end

	local precision = 0
	if seconds_str then
		precision = 5 + math.max( math_mod._precision(seconds_str), 0 );
	elseif minutes_str and minutes_str ~= '' then
		precision = 3 + math.max( math_mod._precision(minutes_str), 0 );
	else
		precision = math.max( math_mod._precision(degrees_str), 0 );
	end

	local decimal = factor * (degrees+(minutes+seconds/60)/60)
	return string.format( "%." .. precision .. "f", decimal ) -- not tonumber since this whole thing is string based.
end

--[[
Checks input values to for out of range errors.
]]
local function validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, source, strong )
	local errors = {};
	lat_d = tonumber( lat_d ) or 0;
	lat_m = tonumber( lat_m ) or 0;
	lat_s = tonumber( lat_s ) or 0;
	long_d = tonumber( long_d ) or 0;
	long_m = tonumber( long_m ) or 0;
	long_s = tonumber( long_s ) or 0;

	if strong then
		if lat_d &lt; 0 then
			table.insert(errors, {source, "latitude degrees &lt; 0 with hemisphere flag"})
		end
		if long_d &lt; 0 then
			table.insert(errors, {source, "longitude degrees &lt; 0 with hemisphere flag"})
		end
		--[[
		#coordinates is inconsistent about whether this is an error.  If globe: is
		specified, it won't error on this condition, but otherwise it will.

		For not simply disable this check.

		if long_d &gt; 180 then
			table.insert(errors, {source, "longitude degrees &gt; 180 with hemisphere flag"})
		end
		]]
	end

	if lat_d &gt; 90 then
		table.insert(errors, {source, "latitude degrees &gt; 90"})
	end
	if lat_d &lt; -90 then
		table.insert(errors, {source, "latitude degrees &lt; -90"})
	end
	if lat_m &gt;= 60 then
		table.insert(errors, {source, "latitude minutes &gt;= 60"})
	end
	if lat_m &lt; 0 then
		table.insert(errors, {source, "latitude minutes &lt; 0"})
	end
	if lat_s &gt;= 60 then
		table.insert(errors, {source, "latitude seconds &gt;= 60"})
	end
	if lat_s &lt; 0 then
		table.insert(errors, {source, "latitude seconds &lt; 0"})
	end
	if long_d &gt;= 360 then
		table.insert(errors, {source, "longitude degrees &gt;= 360"})
	end
	if long_d &lt;= -360 then
		table.insert(errors, {source, "longitude degrees &lt;= -360"})
	end
	if long_m &gt;= 60 then
		table.insert(errors, {source, "longitude minutes &gt;= 60"})
	end
	if long_m &lt; 0 then
		table.insert(errors, {source, "longitude minutes &lt; 0"})
	end
	if long_s &gt;= 60 then
		table.insert(errors, {source, "longitude seconds &gt;= 60"})
	end
	if long_s &lt; 0 then
		table.insert(errors, {source, "longitude seconds &lt; 0"})
	end

	return errors;
end

--[[
parseDec

Transforms decimal format latitude and longitude into the
structure to be used in displaying coordinates
]]
local function parseDec( lat, long, format )
	local coordinateSpec = {}
	local errors = {}

	if not long then
		return nil, {{"parseDec", "Missing longitude"}}
	elseif not tonumber(long) then
		return nil, {{"parseDec", "Longitude could not be parsed as a number: " .. long}}
	end

	errors = validate( lat, nil, nil, long, nil, nil, 'parseDec', false );
	coordinateSpec["dec-lat"]  = lat;
	coordinateSpec["dec-long"] = long;

	local mode = coordinates.determineMode( lat, long );
	coordinateSpec["dms-lat"]  = convert_dec2dms( lat, "N", "S", mode)  -- {{coord/dec2dms|{{{1}}}|N|S|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}
	coordinateSpec["dms-long"] = convert_dec2dms( long, "E", "W", mode)  -- {{coord/dec2dms|{{{2}}}|E|W|{{coord/prec dec|{{{1}}}|{{{2}}}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dec"
	end

	return coordinateSpec, errors
end

--[[
parseDMS

Transforms degrees, minutes, seconds format latitude and longitude
into the a structure to be used in displaying coordinates
]]
local function parseDMS( lat_d, lat_m, lat_s, lat_f, long_d, long_m, long_s, long_f, format )
	local coordinateSpec, errors, backward = {}, {}

	lat_f = lat_f:upper();
	long_f = long_f:upper();

	-- Check if specified backward
	if lat_f == 'E' or lat_f == 'W' then
		lat_d, long_d, lat_m, long_m, lat_s, long_s, lat_f, long_f, backward = long_d, lat_d, long_m, lat_m, long_s, lat_s, long_f, lat_f, true;
	end

	errors = validate( lat_d, lat_m, lat_s, long_d, long_m, long_s, 'parseDMS', true );
	if not long_d then
		return nil, {{"parseDMS", "Missing longitude" }}
	elseif not tonumber(long_d) then
		return nil, {{"parseDMS", "Longitude could not be parsed as a number:" .. long_d }}
	end

	if not lat_m and not lat_s and not long_m and not long_s and #errors == 0 then
		if math_mod._precision( lat_d ) &gt; 0 or math_mod._precision( long_d ) &gt; 0 then
			if lat_f:upper() == 'S' then
				lat_d = '-' .. lat_d;
			end
			if long_f:upper() == 'W' then
				long_d = '-' .. long_d;
			end

			return parseDec( lat_d, long_d, format );
		end
	end

	coordinateSpec["dms-lat"]  = lat_d.."°"..optionalArg(lat_m,"′") .. optionalArg(lat_s,"″") .. lat_f
	coordinateSpec["dms-long"] = long_d.."°"..optionalArg(long_m,"′") .. optionalArg(long_s,"″") .. long_f
	coordinateSpec["dec-lat"]  = convert_dms2dec(lat_f, lat_d, lat_m, lat_s) -- {{coord/dms2dec|{{{4}}}|{{{1}}}|0{{{2}}}|0{{{3}}}}}
	coordinateSpec["dec-long"] = convert_dms2dec(long_f, long_d, long_m, long_s) -- {{coord/dms2dec|{{{8}}}|{{{5}}}|0{{{6}}}|0{{{7}}}}}

	if format then
		coordinateSpec.default = format
	else
		coordinateSpec.default = "dms"
	end

	return coordinateSpec, errors, backward
end

--[[
Check the input arguments for coord to determine the kind of data being provided
and then make the necessary processing.
]]
local function formatTest(args)
	local result, errors
	local backward, primary = false, false

	local function getParam(args, lim)
		local ret = {}
		for i = 1, lim do
			ret[i] = args[i] or ''
		end
		return table.concat(ret, '_')
	end

	if not args[1] then
		-- no lat logic
		return errorPrinter( {{"formatTest", "Missing latitude"}} )
	elseif not tonumber(args[1]) then
		-- bad lat logic
		return errorPrinter( {{"formatTest", "Unable to parse latitude as a number:" .. args[1]}} )
	elseif not args[4] and not args[5] and not args[6] then
		-- dec logic
		result, errors = parseDec(args[1], args[2], args.format)
		if not result then
			return errorPrinter(errors);
		end
		-- formatting for geohack: geohack expects D_N_D_E notation or D;D notation
		-- wikiminiatlas doesn't support D;D notation
		-- #coordinates parserfunction doesn't support negative decimals with NSWE
		result.param = table.concat({
			math.abs(tonumber(args[1])),
			((tonumber(args[1]) or 0) &lt; 0) and 'S' or 'N',
			math.abs(tonumber(args[2])),
			((tonumber(args[2]) or 0) &lt; 0) and 'W' or 'E',
			args[3] or ''}, '_')
	elseif dmsTest(args[4], args[8]) then
		-- dms logic
		result, errors, backward = parseDMS(args[1], args[2], args[3], args[4],
			args[5], args[6], args[7], args[8], args.format)
		if args[10] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 9)
	elseif dmsTest(args[3], args[6]) then
		-- dm logic
		result, errors, backward = parseDMS(args[1], args[2], nil, args[3],
			args[4], args[5], nil, args[6], args['format'])
		if args[8] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 7)
	elseif dmsTest(args[2], args[4]) then
		-- d logic
		result, errors, backward = parseDMS(args[1], nil, nil, args[2],
			args[3], nil, nil, args[4], args.format)
		if args[6] then
			table.insert(errors, {'formatTest', 'Extra unexpected parameters'})
		end
		if not result then
			return errorPrinter(errors)
		end
		result.param = getParam(args, 5)
	else
		-- Error
		return errorPrinter({{"formatTest", "Unknown argument format"}}) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	result.name = args.name

	local extra_param = {'dim', 'globe', 'scale', 'region', 'source', 'type'}
	for _, v in ipairs(extra_param) do
		if args[v] then
			table.insert(errors, {'formatTest', 'Parameter: "' .. v .. '=" should be "' .. v .. ':"' })
		end
	end

	local ret = specPrinter(args, result)
	if #errors &gt; 0 then
		ret = ret .. ' ' .. errorPrinter(errors) .. '[[Category:Pages with malformed coordinate tags]]'
	end
	return ret, backward
end

--[[
Generate Wikidata tracking categories.
]]
local function makeWikidataCategories(qid)
	local ret
	local qid = qid or mw.wikibase.getEntityIdForCurrentPage()
	if mw.wikibase and current_page.namespace == 0 then
		if qid and mw.wikibase.entityExists(qid) and mw.wikibase.getBestStatements(qid, "P625") and mw.wikibase.getBestStatements(qid, "P625")[1] then
			local snaktype = mw.wikibase.getBestStatements(qid, "P625")[1].mainsnak.snaktype
			if snaktype == 'value' then
				-- coordinates exist both here and on Wikidata, and can be compared.
				ret = 'Coordinates on Wikidata'
			elseif snaktype == 'somevalue' then
				ret = 'Coordinates on Wikidata set to unknown value'
			elseif snaktype == 'novalue' then
				ret = 'Coordinates on Wikidata set to no value'
			end
		else
			-- We have to either import the coordinates to Wikidata or remove them here.
			ret = 'Coordinates not on Wikidata'
		end
	end
	if ret then
		return string.format('[[Category:%s]]', ret)
	else
		return ''
	end
end

--[[
link

Simple function to export the coordinates link for other uses.

Usage:
	{{#invoke:Coordinates | link }}

]]
function coordinates.link(frame)
	return coord_link;
end

--[[
dec2dms

Wrapper to allow templates to call dec2dms directly.

Usage:
	{{#invoke:Coordinates | dec2dms | decimal_coordinate | positive_suffix |
		negative_suffix | precision }}

decimal_coordinate is converted to DMS format.  If positive, the positive_suffix
is appended (typical N or E), if negative, the negative suffix is appended.  The
specified precision is one of 'D', 'DM', or 'DMS' to specify the level of detail
to use.
]]
coordinates.dec2dms = makeInvokeFunc('_dec2dms')
function coordinates._dec2dms(args)
	local coordinate = args[1]
	local firstPostfix = args[2] or ''
	local secondPostfix = args[3] or ''
	local precision = args[4] or ''

	return convert_dec2dms(coordinate, firstPostfix, secondPostfix, precision)
end

--[[
Helper function to determine whether to use D, DM, or DMS
format depending on the precision of the decimal input.
]]
function coordinates.determineMode( value1, value2 )
	local precision = math.max( math_mod._precision( value1 ), math_mod._precision( value2 ) );
	if precision &lt;= 0 then
		return 'd'
	elseif precision &lt;= 2 then
		return 'dm';
	else
		return 'dms';
	end
end

--[[
dms2dec

Wrapper to allow templates to call dms2dec directly.

Usage:
	{{#invoke:Coordinates | dms2dec | direction_flag | degrees |
		minutes | seconds }}

Converts DMS values specified as degrees, minutes, seconds too decimal format.
direction_flag is one of N, S, E, W, and determines whether the output is
positive (i.e. N and E) or negative (i.e. S and W).
]]
coordinates.dms2dec = makeInvokeFunc('_dms2dec')
function coordinates._dms2dec(args)
	local direction = args[1]
	local degrees = args[2]
	local minutes = args[3]
	local seconds = args[4]

	return convert_dms2dec(direction, degrees, minutes, seconds)
end

--[[
coord

Main entry point for Lua function to replace {{coord}}

Usage:
	{{#invoke:Coordinates | coord }}
	{{#invoke:Coordinates | coord | lat | long }}
	{{#invoke:Coordinates | coord | lat | lat_flag | long | long_flag }}
	...

	Refer to {{coord}} documentation page for many additional parameters and
	configuration options.

Note: This function provides the visual display elements of {{coord}}.  In
order to load coordinates into the database, the {{#coordinates:}} parser
function must also be called, this is done automatically in the Lua
version of {{coord}}.
]]
coordinates.coord = makeInvokeFunc('_coord')
function coordinates._coord(args)
	if not tonumber(args[1]) and not args[2] then
		args[3] = args[1]; args[1] = nil
		local entity = mw.wikibase.getEntityObject(args.qid)
		if entity
			and entity.claims
			and entity.claims.P625
			and entity.claims.P625[1].mainsnak.snaktype == 'value'
		then
			local precision = entity.claims.P625[1].mainsnak.datavalue.value.precision
			args[1] = entity.claims.P625[1].mainsnak.datavalue.value.latitude
			args[2] = entity.claims.P625[1].mainsnak.datavalue.value.longitude
			if precision then
				precision = -math_mod._round(math.log(precision)/math.log(10),0)
				args[1] = math_mod._round(args[1],precision)
				args[2] = math_mod._round(args[2],precision)
			end
		end
	end

	local contents, backward = formatTest(args)
	local Notes = args.notes or ''
	local Display = args.display and args.display:lower() or 'inline'

	-- it and ti are short for inline,title and title,inline
	local function isInline(s)
		-- Finds whether coordinates are displayed inline.
		return s:find('inline') ~= nil or s == 'i' or s == 'it' or s == 'ti'
	end
	local function isInTitle(s)
		-- Finds whether coordinates are displayed in the title.
		return s:find('title') ~= nil or s == 't' or s == 'it' or s == 'ti'
	end

	local function coord_wrapper(in_args)
		-- Calls the parser function {{#coordinates:}}.
		return mw.getCurrentFrame():callParserFunction('#coordinates', in_args) or ''
	end

	local text = ''
	if isInline(Display) then
		text = text .. '&lt;span class="geo-inline"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
	end
	if isInTitle(Display) then
		-- Add to output since indicator content is invisible to Lua later on
		if not isInline(Display) then
			text = text .. '&lt;span class="geo-inline-hidden noexcerpt"&gt;' .. contents .. Notes .. '&lt;/span&gt;'
		end
		text = text .. displaytitle(contents .. Notes) .. makeWikidataCategories(args.qid)
	end
	if not args.nosave then
		local page_title, count = mw.title.getCurrentTitle(), 1
		if backward then
			local tmp = {}
			while not string.find((args[count-1] or ''), '[EW]') do tmp[count] = (args[count] or ''); count = count+1 end
			tmp.count = count; count = 2*(count-1)
			while count &gt;= tmp.count do table.insert(tmp, 1, (args[count] or '')); count = count-1 end
			for i, v in ipairs(tmp) do args[i] = v end
		else
			while count &lt;= 9 do args[count] = (args[count] or ''); count = count+1 end
		end
		if isInTitle(Display) and not page_title.isTalkPage and page_title.subpageText ~= 'doc' and page_title.subpageText ~= 'testcases' then args[10] = 'primary' end
		args.notes, args.format, args.display = nil
		text = text .. coord_wrapper(args)
	end
	return text
end

--[[
coord2text

Extracts a single value from a transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coord2text | {{Coord}} | parameter }}

Valid values for the second parameter are: lat (signed integer), long (signed integer), type, scale, dim, region, globe, source

]]
function coordinates._coord2text(coord,type)
	if coord == '' or type == '' or not type then return nil end
	type = mw.text.trim(type)
	if type == 'lat' or type == 'long' then
        local coordString = mw.ustring.match(coord,'[%.%d]+°[NS] [%.%d]+°[EW]')
        if not coordString then
            return nil
        end
		local result = mw.text.split(coordString, ' ')
        local negative
		if type == 'lat' then
			result, negative = result[1], 'S'
		else
			result, negative = result[2], 'W'
		end
        if not result then
            return nil
        end
		result = mw.text.split(result, '°')
        if not result[1] then
            return nil
        end
		if result[2] == negative then result[1] = '-'..result[1] end
		return result[1]
	else
		return mw.ustring.match(coord, 'params=.-_' .. type .. ':(.-)[ _]')
	end
end

function coordinates.coord2text(frame)
	return coordinates._coord2text(frame.args[1],frame.args[2]) or ''
end

--[[
coordinsert

Injects some text into the Geohack link of a transclusion of {{Coord}} (if that text isn't already in the transclusion). Outputs the modified transclusion of {{Coord}}.
IF THE GEOHACK LINK SYNTAX CHANGES THIS FUNCTION MUST BE MODIFIED.

Usage:

    {{#invoke:Coordinates | coordinsert | {{Coord}} | parameter:value | parameter:value | … }}

Do not make Geohack unhappy by inserting something which isn't mentioned in the {{Coord}} documentation.

]]
function coordinates.coordinsert(frame)
	-- for the 2nd or later integer parameter (the first is the coord template, as above)
	for i, v in ipairs(frame.args) do
		if i ~= 1 then
			-- if we cannot find in the coord_template the i_th coordinsert parameter e.g. region
			if not mw.ustring.find(frame.args[1], (mw.ustring.match(frame.args[i], '^(.-:)') or '')) then
				-- find from the params= up to the first possibly-present underscore
				-- and append the i_th coordinsert parameter and a space
				-- IDK why we're adding a space but it does seem somewhat convenient
				frame.args[1] = mw.ustring.gsub(frame.args[1], '(params=.-)_? ', '%1_'..frame.args[i]..' ')
			end
		end
	end
	if frame.args.name then
		-- if we can't find the vcard class
		if not mw.ustring.find(frame.args[1], '&lt;span class="vcard"&gt;') then
			-- take something that looks like a coord template and add the vcard span with class and fn org class
			local namestr = frame.args.name
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&lt;span class="geo%-default"&gt;)(&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;span[^&lt;&gt;]*&gt;[^&lt;&gt;]*&lt;/span&gt;&lt;/span&gt;)(&lt;/span&gt;)',
				'%1&lt;span class="vcard"&gt;%2&lt;span style="display:none"&gt;&amp;#xfeff; (&lt;span class="fn org"&gt;' .. namestr .. '&lt;/span&gt;)&lt;/span&gt;&lt;/span&gt;%3'
			)
			-- then find anything from coordinates parameters to the 'end' and attach the title parameter
			frame.args[1] = mw.ustring.gsub(
				frame.args[1],
				'(&amp;params=[^&amp;"&lt;&gt;%[%] ]*) ',
				'%1&amp;title=' .. mw.uri.encode(namestr) .. ' '
			)
		end
	end
	
	-- replace the existing indicator with a new indicator using the modified content
	frame.args[1] = mw.ustring.gsub(
		frame.args[1],
		'(&lt;span class="geo%-inline[^"]*"&gt;(.+)&lt;/span&gt;)\127[^\127]*UNIQ%-%-indicator%-%x+%-%-?QINU[^\127]*\127',
		function (inline, coord) return inline .. displaytitle(coord) end
	)

	return frame.args[1]
end

return coordinates</text>
      <sha1>4j0gstvbrnunfsl9d87kcwlvt5s11ed</sha1>
    </revision>
  </page>
</mediawiki>
